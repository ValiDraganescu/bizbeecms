# ProjectManager v0.12.0

_2026-08-21 · changes since pm-v0.11.0_

## Features
- Deleting a Site in local development now also removes its local CMS state (the per-Site Miniflare D1/R2 under `CMS/.wrangler/sites/<slug>/`), so the local CMS can't keep serving a Site the PM no longer knows about. No-op in production.

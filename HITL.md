# HITL — things the human needs to do

Human-in-the-loop task log. Meeseeks workers append here instead of stopping
when they hit something only the user can do (live CF auth, a manual test, a
secret, an external account action, a judgment call). The user clears this list
and tests. Newest at the top.

Format: `- [ ] [PRIORITY] <what the user must do> — why: <reason> — added <YYYY-MM-DD> by <task>`
Priority: P0 blocks the goal · P1 blocks a feature · P2 nice-to-have / verification.

## Open

- [ ] [P1] **Live-verify the CMS admin-auth bridge (Sec1)** once a CMS Worker is deployed. Prereqs the deployer/secrets must supply: set `CMS_AUTH_SECRET` (shared bearer) + `PM_ORIGIN` on the **deployer** Worker, and confirm the deployer's `wrangler deploy --var SITE_ID:… --var PM_ORIGIN:… --var CMS_AUTH_SECRET:…` actually lands those as Worker vars on the deployed CMS (overriding the empty placeholders in `CMS/wrangler.jsonc`). Then test: (1) hit any admin route signed-out, e.g. `curl -i https://<cms>/api/pages` → expect **401** (and `GET /admin/pages` → redirect to `<PM_ORIGIN>/login`); (2) sign in to PM as a user WITH access to that Site, then hit the CMS forwarding the `bizbee_session` cookie → expect **200** (admin pages render); (3) a PM user WITHOUT access to that Site → expect **401** / the "Access denied" forbidden page. Also confirm PM's `POST /api/auth/cms-validate` returns `{ok:true,userId}` for an authorized cookie+siteId and `{ok:false}`/401 otherwise. WHY: the cross-Worker fetch + the deployer var injection + the shared secret can't be exercised offline — only the pure guard/decision logic + reach parity are unit-tested. — added 2026-06-18 by Sec1 CMS admin auth
- [ ] [P1] Provision a per-Site **R2 bucket** for the CMS Worker (epic D1, media library) and confirm the `MEDIA` binding works: the CMS `wrangler.jsonc` declares `r2_buckets: [{binding:"MEDIA", bucket_name:"bizbeecms-cms-media"}]` (placeholder name; the PM/deployer should override it per-Site, like the per-Site D1). Then live-test: open `/admin/media`, upload an image → expect a 201 + the image shows in the gallery + `GET /media/assets/...` returns the bytes (cache-control immutable) + "Copy URL" gives `/media/<key>` usable in a component. WHY: R2 put/get/delete need a real bucket binding; can't run offline. Migration `CMS/migrations/0002_cool_deathbird.sql` adds the `asset` metadata table — apply it (`wrangler d1 migrations apply`) alongside the bucket. — added 2026-06-17 by D1 R2 asset gallery
- [ ] [P1] Live-test the **B2 create_component tool** once a CMS Worker is deployed (needs the AI binding + gateway + a real D1): POST `/api/chat` with `{"messages":[{"role":"user","content":"create a component named PricingCard with a heading and a paragraph"}]}` and confirm an `event: tool {"name":"create_component","ok":true,"action":"created","component":"PricingCard"}` frame arrives, then a page using it renders. WHY: the model's tool-calling reliability + the live D1 write can't run offline. If `@cf/meta/llama-3.1-8b-instruct` won't reliably emit a `create_component` tool call with a valid `{tree}` (BACKLOG B1 risk — open models are weak at tool use), point the AI Gateway at a stronger model (no re-architecture; swap `DEFAULT_MODEL`/gateway in `route.ts`). NOTE: the route runs a SINGLE tool round (validate → D1 upsert → `tool` event); the full multi-turn agentic loop (feed the tool result back for a follow-up model turn) is deferred — assess whether one round suffices live before building it. — added 2026-06-17 by B2 create-component tool
- [ ] [P1] Create an **AI Gateway** named `bizbeecms-cms` in the Cloudflare dashboard (AI → AI Gateway) for the CMS Worker's account, and confirm **Workers AI** is enabled — why: the CMS chat endpoint (`/api/chat`, epic B1) calls `env.AI.run(..., { gateway: { id: "bizbeecms-cms" } })`; the binding+gateway can't be exercised offline. Override the gateway slug per-Site via the `AI_GATEWAY` var if you use a different name. — added 2026-06-17 by B1 chat endpoint
- [ ] [P1] Live-test the **B4 translate tool** once a CMS Worker is deployed (needs the AI binding + gateway + real D1 with a page): create a page (B3), then POST `/api/chat` with `{"messages":[{"role":"user","content":"translate the pricing page metaTitle and the hero1.title block prop into Finnish"}]}` and confirm an `event: tool {"name":"translate","ok":true,"action":"translated","target":"pricing","fields":N}` frame arrives, then the page renders the Finnish text under `?locale=fi` (resolveLocalized, C1). WHY: the model's tool-calling + the live D1 read-merge-write can't run offline. The tool constrains the model to the Site's configured content locales (`getContentLocales`); set those first (no UI yet — C1b is the next slice). component-target translation is intentionally rejected for now (translate the page whose blocks use the component). — added 2026-06-17 by B4 translate tool
- [ ] [P2] Live-test the CMS chat SSE stream once a CMS Worker is deployed with the `AI` binding: `curl -N -X POST https://<cms-worker>/api/chat -H 'content-type: application/json' -d '{"messages":[{"role":"user","content":"say hi"}]}'` — expect `event: token` frames then `event: done`. Verify the model `@cf/meta/llama-3.1-8b-instruct` exists/streams; swap in `route.ts` if not. — added 2026-06-17 by B1 chat endpoint

## Done

- [x] [P0] **CMS admin auth — DECIDED 2026-06-18 by the user.** Resolution: option (a), reuse PM's EXACT auth mechanism (opaque `bizbee_session` cookie → KV `SESSIONS` session record → userId), NOT standalone CMS auth. Authorization: **any PM user with access to the Site IS a CMS admin** for that Site's Worker — mirror PM's reach check (`canManageSiteByCountry(user, site) || isUserAssignedToSite(userId, siteId)`, `ProjectManager/src/lib/site/site.ts:138`). Bridge (CMS can't reach PM's KV/D1 directly): **(1) CMS calls a new PM endpoint** `POST /api/auth/cms-validate` forwarding the `bizbee_session` cookie + `{siteId}` + a shared bearer secret (reuse the deployer-callback pattern); PM resolves the session in its KV, runs its own access check, returns `{ok, userId}`. **(2) Site identity:** deployer injects `SITE_ID` as a plain Worker var in the CMS wrangler override (it already passes SITE_ID to the build — just also set it as an env var); the CMS guard reads `env.SITE_ID` and sends it as `{siteId}`. This is now the **Sec1 epic** — build spec lives in goal `NEXT.md`. — resolved 2026-06-18 (was added 2026-06-17 by D1 R2 asset gallery security review)

(user moves items here after handling them)

# HITL — things the human needs to do

Human-in-the-loop task log. Meeseeks workers append here instead of stopping
when they hit something only the user can do (live CF auth, a manual test, a
secret, an external account action, a judgment call). The user clears this list
and tests. Newest at the top.

Format: `- [ ] [PRIORITY] <what the user must do> — why: <reason> — added <YYYY-MM-DD> by <task>`
Priority: P0 blocks the goal · P1 blocks a feature · P2 nice-to-have / verification.

## Open

- [ ] [P1] **Set `SITE_SECRET_KEY` on the PM Worker** so per-Site OpenRouter key encryption works live (per-Site OpenRouter key track, Slice 1). Run `wrangler secret put SITE_SECRET_KEY` in `ProjectManager/` and paste a 32-byte base64 value (`openssl rand -base64 32`). WHY: it's the AES-256-GCM KEK that `src/lib/crypto/secret-box.ts` encrypts/decrypts each Site's OpenRouter key with; without it encryption throws at runtime. Keep the SAME value forever — rotating it makes already-stored encrypted keys undecryptable. — added 2026-06-22 by per-Site OpenRouter key Slice 1
- [ ] [P1] **Provision the CMS "Sign in with Google" OAuth client (cms-auth Slice 2b) + live-test the round-trip.** The offline code (start/callback routes, signed-state CSRF, id_token email verify, no-self-signup decision, deployer var injection, EN/FI/ET, gate) is landed; only the live Google handshake is HITL. Steps: (1) In Google Cloud Console create an **OAuth 2.0 Web client**; set the **Authorized redirect URI** to EXACTLY `<APP_ORIGIN>/api/auth/google/callback` for each deployed CMS (APP_ORIGIN = the per-Site `https://bizbeecms-cms-<slug>.<account>.workers.dev`) — Google rejects any mismatch. For multiple Sites either add each callback URI to one client or use a per-Site client. (2) Set the deployer secrets: `wrangler secret put GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` on the `bizbeecms-deployer` Worker (the deployer injects them as CMS Worker `--var`s at deploy time). (3) **Redeploy a Site** (vars bake at `wrangler deploy` time). (4) Test: on the CMS login page the **"Sign in with Google"** button appears (only when GOOGLE_CLIENT_ID + APP_ORIGIN are set); signing in with a Google account whose email matches an existing CMS user → lands on `/admin` signed in; an **uninvited** Google email → `/admin?error=googleDenied` banner (no self-signup); an email with only a pending invite → `?error=googleInvitePending` (must finish via the invite link). WHY: the live Google consent + token exchange + redirect-URI registration can't run offline — only the pure helpers (state sign/verify, id_token decode, decision) are unit-tested. — added 2026-06-22 by cms-auth Slice 2b Google sign-in
- [ ] [P1] **Live-verify the CMS admin-auth bridge (Sec1)** once a CMS Worker is deployed. (✅ RESOLVED 2026-06-18: `CMS_AUTH_SECRET` is now set on BOTH the PM and deployer Workers with the same value; `PM_ORIGIN` resolves via the deployer's `PM_CALLBACK_ORIGIN` fallback — see `DEPLOY-ARCHITECTURE.md`.) Remaining: confirm the deployer's `wrangler deploy --var SITE_ID:… --var PM_ORIGIN:… --var CMS_AUTH_SECRET:…` actually lands those as Worker vars on the deployed CMS (overriding the empty placeholders in `CMS/wrangler.jsonc`). Then test: (1) hit any admin route signed-out, e.g. `curl -i https://<cms>/api/pages` → expect **401** (and `GET /admin/pages` → redirect to `<PM_ORIGIN>/login`); (2) sign in to PM as a user WITH access to that Site, then hit the CMS forwarding the `bizbee_session` cookie → expect **200** (admin pages render); (3) a PM user WITHOUT access to that Site → expect **401** / the "Access denied" forbidden page. Also confirm PM's `POST /api/auth/cms-validate` returns `{ok:true,userId}` for an authorized cookie+siteId and `{ok:false}`/401 otherwise. WHY: the cross-Worker fetch + the deployer var injection + the shared secret can't be exercised offline — only the pure guard/decision logic + reach parity are unit-tested. — added 2026-06-18 by Sec1 CMS admin auth
- [ ] [P1] Provision a per-Site **R2 bucket** for the CMS Worker (epic D1, media library) and confirm the `MEDIA` binding works: the CMS `wrangler.jsonc` declares `r2_buckets: [{binding:"MEDIA", bucket_name:"bizbeecms-cms-media"}]` (placeholder name; the PM/deployer should override it per-Site, like the per-Site D1). Then live-test: open `/admin/media`, upload an image → expect a 201 + the image shows in the gallery + `GET /media/assets/...` returns the bytes (cache-control immutable) + "Copy URL" gives `/media/<key>` usable in a component. WHY: R2 put/get/delete need a real bucket binding; can't run offline. Migration `CMS/migrations/0002_cool_deathbird.sql` adds the `asset` metadata table — apply it (`wrangler d1 migrations apply`) alongside the bucket. — added 2026-06-17 by D1 R2 asset gallery
- [ ] [P1] Live-test the **B2 create_component tool** once a CMS Worker is deployed (needs the AI binding + gateway + a real D1): POST `/api/chat` with `{"messages":[{"role":"user","content":"create a component named PricingCard with a heading and a paragraph"}]}` and confirm an `event: tool {"name":"create_component","ok":true,"action":"created","component":"PricingCard"}` frame arrives, then a page using it renders. WHY: the model's tool-calling reliability + the live D1 write can't run offline. If `@cf/meta/llama-3.1-8b-instruct` won't reliably emit a `create_component` tool call with a valid `{tree}` (BACKLOG B1 risk — open models are weak at tool use), point the AI Gateway at a stronger model (no re-architecture; swap `DEFAULT_MODEL`/gateway in `route.ts`). NOTE: the route runs a SINGLE tool round (validate → D1 upsert → `tool` event); the full multi-turn agentic loop (feed the tool result back for a follow-up model turn) is deferred — assess whether one round suffices live before building it. — added 2026-06-17 by B2 create-component tool
- [ ] [P1] **REDEPLOY the live CMS Site** so it picks up the corrected AI Gateway slug (BUG P1 fix 2026-06-19): `AI_GATEWAY` is now `bizbeecms-ai-gateway` (was the wrong `bizbeecms-cms` → every chat message errored `2001: Please configure AI Gateway`). The var is baked at `wrangler deploy` time, so the already-deployed CMS still has the old slug until redeployed (PM → Site → Redeploy). The gateway `bizbeecms-ai-gateway` already exists on the account (proven by the user's working curl). After redeploy, confirm a chat message no longer errors 2001. — updated 2026-06-19 by BUG-P1 gateway-slug fix (supersedes the old "create gateway named bizbeecms-cms" item)
- [ ] [P1] Live-test the **B4 translate tool** once a CMS Worker is deployed (needs the AI binding + gateway + real D1 with a page): create a page (B3), then POST `/api/chat` with `{"messages":[{"role":"user","content":"translate the pricing page metaTitle and the hero1.title block prop into Finnish"}]}` and confirm an `event: tool {"name":"translate","ok":true,"action":"translated","target":"pricing","fields":N}` frame arrives, then the page renders the Finnish text under `?locale=fi` (resolveLocalized, C1). WHY: the model's tool-calling + the live D1 read-merge-write can't run offline. The tool constrains the model to the Site's configured content locales (`getContentLocales`); set those first (no UI yet — C1b is the next slice). component-target translation is intentionally rejected for now (translate the page whose blocks use the component). — added 2026-06-17 by B4 translate tool
- [ ] [P1] **Live-verify a per-Site OpenRouter key reaches a deployed CMS (ai-openrouter Slice 4).** Offline-complete: the deployer now sets `OPENROUTER_API_KEY` as a CMS Worker **secret** (`printf '%s' "$KEY" | wrangler secret put OPENROUTER_API_KEY --name <worker>`) after `wrangler deploy` succeeds — dropped from `--var` — using the per-Site key from the deploy POST body when present, else the deployer's global `OPENROUTER_API_KEY`, else skips the secret entirely. The live `wrangler secret put` can't run offline. Steps: (1) In PM, set a Site's own OpenRouter key (Site settings → write-only key field). (2) Redeploy that Site (PM → Site → Redeploy). (3) Confirm the deploy succeeds and a `secret` step runs. (4) On the deployed CMS, send a chat message and confirm completions stream via OpenRouter using THAT Site's key (e.g. check OpenRouter dashboard attribution / that a Site with NO key still falls back to the deployer global or Workers AI). WHY: the secret-put + live OpenRouter call need a real deploy + real keys; only the effective-key decision is unit-tested. — added 2026-06-22 by ai-openrouter Slice 4 (deployer per-Site key secret)
- [ ] [P2] Live-test the CMS chat SSE stream once a CMS Worker is deployed with the `AI` binding: `curl -N -X POST https://<cms-worker>/api/chat -H 'content-type: application/json' -d '{"messages":[{"role":"user","content":"say hi"}]}'` — expect `event: token` frames then `event: done`. Verify the model `@cf/meta/llama-3.1-8b-instruct` exists/streams; swap in `route.ts` if not. — added 2026-06-17 by B1 chat endpoint

## Done

- [x] [P1] ✅ VERIFIED 2026-06-24 by the user — minting works end-to-end. **Live-verify per-Site OpenRouter KEY MINTING + CMS-local override end-to-end (ai-openrouter — final HITL).** Everything codeable is done & unit-covered (PM 187/187, CMS 776/776, both `npx opennextjs-cloudflare build` green, 2026-06-23). PREREQ: ✅ DONE 2026-06-24 — `OPENROUTER_PROVISIONING_KEY` is set on the **PM** Worker (a single OpenRouter Management/Provisioning key — used only to mint/delete per-Site keys; without it `mintKey` throws → caught → deploy falls back to the global key, never 500s). Steps verified: (1) PM Edit Site → toggle minting ON + monthly USD limit → Deploy mints a `sk-or-...` key named after the Site slug, "minted" hint + enabled "Delete current key" appear, key reaches the CMS Worker as the `OPENROUTER_API_KEY` secret. (2) Idempotency: redeploy does NOT re-mint (`shouldMintOnDeploy` skips on non-null `openrouterKeyHash`). (3) Chat streams via OpenRouter using the minted key. (4) Delete revokes via DELETE `/api/v1/keys/:hash` + clears PM state (proceed-and-clear on 404). (5) CMS-local user key overrides the minted/deployer key at request time (precedence: CMS-local → minted/global env → CfAi → 503). — added 2026-06-23 by ai-openrouter Verify-minting-end-to-end

- [x] [P0] **CMS admin auth — DECIDED 2026-06-18 by the user.** Resolution: option (a), reuse PM's EXACT auth mechanism (opaque `bizbee_session` cookie → KV `SESSIONS` session record → userId), NOT standalone CMS auth. Authorization: **any PM user with access to the Site IS a CMS admin** for that Site's Worker — mirror PM's reach check (`canManageSiteByCountry(user, site) || isUserAssignedToSite(userId, siteId)`, `ProjectManager/src/lib/site/site.ts:138`). Bridge (CMS can't reach PM's KV/D1 directly): **(1) CMS calls a new PM endpoint** `POST /api/auth/cms-validate` forwarding the `bizbee_session` cookie + `{siteId}` + a shared bearer secret (reuse the deployer-callback pattern); PM resolves the session in its KV, runs its own access check, returns `{ok, userId}`. **(2) Site identity:** deployer injects `SITE_ID` as a plain Worker var in the CMS wrangler override (it already passes SITE_ID to the build — just also set it as an env var); the CMS guard reads `env.SITE_ID` and sends it as `{siteId}`. This is now the **Sec1 epic** — build spec lives in goal `NEXT.md`. — resolved 2026-06-18 (was added 2026-06-17 by D1 R2 asset gallery security review)

(user moves items here after handling them)

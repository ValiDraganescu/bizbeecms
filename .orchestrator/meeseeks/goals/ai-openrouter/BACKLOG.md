# Backlog — ai-openrouter
Task states: TODO | DOING | DONE | BLOCKED.

## Bugs
(human-reported bugs land here, newest at top; they outrank everything)

## per-Site OpenRouter key TRACK
- DONE: **Slice 4 — deployer sets the per-Site key as a CMS Worker SECRET.** `deployer/src/index.ts`: `DeployBody.openrouterApiKey?` read in the `/deploy` handler → `startDeploy`. Pure `effectiveOpenrouterKey(perSite, global)` picks perSite-non-empty ?? global ?? "" and whether to set the secret (non-empty). Bash DROPS `--var OPENROUTER_API_KEY`; after `wrangler deploy` succeeds, `printf '%s' "$KEY" | wrangler secret put OPENROUTER_API_KEY --name "$WORKER_NAME"` (gated on a flag so blank keys skip). Value via stdin, never logged. Gate: `wrangler deploy --dry-run` clean + `scripts/openrouter-key.test.mjs` (8 assertions). TRACK COMPLETE; HITL live-verify in root HITL.md.
- DONE: **Slice 3 — thread the decrypted Site key into the deploy POST.** Deploy route loads `site.openrouterApiKeyEncrypted`; non-null → `decryptSecret(it, env.SITE_SECRET_KEY)` and add `openrouterApiKey: <plaintext>` to the deployer POST body; null → omit. Decrypt failure (bad/rotated/unset key, corrupt blob) MUST NOT crash the deploy — catch, warn, omit, proceed (graceful degrade to deployer global key). Pure helper decides the body field from (encryptedOrNull, decryptResultOrThrow); dep-free `.mjs` test. Body field = `openrouterApiKey`.

## Tasks
Ordered tracer-first: prove the adapter + test in isolation, then wire selection + secret, then swap the catalog, then verify end-to-end. Each is one Meeseeks slice.

- DONE: **Tracer — OpenRouter `Ai` adapter behind the existing port.** Add `OpenRouterAi` in `CMS/src/lib/ports/ai.ts` (or a sibling) implementing the same streaming OpenAI-compatible contract as `CfAi`, calling `https://openrouter.ai/api/v1/chat/completions` with `Authorization: Bearer ${OPENROUTER_API_KEY}` (preserve `stream: true` + the `tools` array, return the raw upstream SSE stream). Update the port's "no second adapter / CF-native" doc comment (~lines 6–9) to record the OpenRouter decision. Unit-test it against a FAKE `fetch` like `CMS/scripts/ai-port.test.mjs` — no live key. Do NOT wire it in yet.
- DONE: **Select the provider in `getAi()` + wire the secret.** `getAi()` now uses pure `pickSelection(env)` — OpenRouter when `OPENROUTER_API_KEY` non-empty (DEFAULT), else CfAi when `AI` binding present, else null→503. `OPENROUTER_API_KEY` declared (empty placeholder) in `CMS/wrangler.jsonc` and injected per-Site by the deployer (`deployer/src/index.ts`: Env type + process env + `--var OPENROUTER_API_KEY:$OPENROUTER_API_KEY`, alongside CMS_AUTH_SECRET/PM_ORIGIN). 3 new selection tests in `openrouter-ai.test.mjs` (7 total green); CMS+deployer tsc 0. Deployer needs `wrangler secret put OPENROUTER_API_KEY` before a live deploy.
- DONE: **Point the model catalog at OpenRouter.** `models.ts` rewritten for the OpenRouter shape: `parseModelCatalog` reads `{ data: [{ id, name, pricing: { prompt } }] }` (also bare array), `providerOf` takes the FIRST `vendor/model` segment, `priceOf` reads `pricing.prompt` (USD/token), `DEFAULT_MODEL = openai/gpt-4o-mini`, static `CHAT_MODELS` = 4 OpenRouter chat models. `GET /api/chat/models` fetches `https://openrouter.ai/api/v1/models` (public; sends `env.OPENROUTER_API_KEY` Bearer when present, via the same env boundary) keeping the D1 cache + 12h lazy refresh + static fallback. Chat route's untrusted-`model` validation unchanged — `resolveModel` against cached ids + static → DEFAULT_MODEL; now resolves to OpenRouter ids automatically. `models.test.mjs` rewritten (12 tests green). tsc 0 in my files (2 pre-existing errors in the parallel worker's `components-manager.tsx`, not mine).
- DONE (offline; live deploy = HITL): **Verify end-to-end.** 2026-06-23: dev OFF, `npx opennextjs-cloudflare build` green; OpenRouter unit tests 23/23; full CMS suite 748/748. Live chat-stream + tool-call round-trip on a deployed CMS is the only non-codeable bit (needs deployer `OPENROUTER_API_KEY` secret) → root HITL.md.

## Unify translate route onto OpenRouter
- DONE (2026-06-23): **Translate route no longer hardcodes a `@cf/...` DEFAULT_MODEL.** `CMS/src/app/api/translate/route.ts` already called `getAi()` (port → OpenRouter when keyed) but kept its own `@cf/meta/llama-3.1-8b-instruct` const → 502 against the OpenRouter adapter on every keyed Site. Replaced with `import { DEFAULT_MODEL } from "@/lib/chat/models"` (= `openai/gpt-4o-mini`), same as the chat route. Regression in `scripts/translate-request.test.mjs` asserts the import + no `@cf/` id in the route source. Gate: translate tests 13/13, full CMS suite 777/777, tsc 0 in my files, `npx opennextjs-cloudflare build` GREEN.

## Operator visibility
- DONE (2026-06-23): **Surface mint failures to the PM operator.** Deploy route now returns
  `mintWarning: true` (alongside `accepted: true`) when mint-on-deploy was attempted and failed; the
  deploy form shows a non-blocking `tone="warning"` Alert (`sites.deploy.mintWarning`, EN/FI/ET).
  Minting still never crashes the deploy (graceful fallback to the deployer global key unchanged).
  Regression `scripts/deploy-mint-warning.test.mjs` (5). Gate: tsc 0, `npm test` 192/192, build GREEN.

## KEY-MINTING TRACK (auto-provision per-Site OpenRouter keys)
Replace the PM manual-paste field with PM-side auto-minting via OpenRouter's Provisioning API, plus a CMS-local user override. Final precedence at request time in the CMS: **CMS-local user key → PM minted key → deployer global fallback**. USER DECISIONS (2026-06-23): minting REPLACES the manual paste field in PM (no more pasting); CMS stores its own key locally in CMS D1 and prefers it at request time; PM gets a per-site spend-limit field for the minted key. Provisioning auth = a single OpenRouter Management/Provisioning key held by PM (NOT per-site), used only to mint/delete site keys. Docs: https://openrouter.ai/docs/features/provisioning-api-keys (POST/DELETE `/api/v1/keys`).

- DONE: **Tracer — pure OpenRouter provisioning client (mint + delete), fake-`fetch` tested.** `ProjectManager/src/lib/openrouter/provision.ts`: `mintKey(provisioningKey, {name, limit?}, fetch?)` → `POST https://openrouter.ai/api/v1/keys` (`Bearer`, `{name, limit?}`; limit OMITTED when null/undefined) returns `{ key: "sk-or-...", hash }` (parsed from `{ key, data: { hash } }`); `deleteKey(provisioningKey, hash, fetch?)` → `DELETE /api/v1/keys/:hash` (hash URL-encoded). Both throw on non-2xx, missing creds (no fetch call), or missing key/hash. `fetch` injected (defaults global). 9 dep-free tests in `scripts/openrouter-provision.test.mjs`. `OPENROUTER_PROVISIONING_KEY` declared as a SECRET comment in `ProjectManager/wrangler.jsonc`. NOT wired into deploy. Gate: `node --test` 9/9, `tsc --noEmit` clean, `npm test` 182/182.
- DONE: **PM schema — minting state on the Site.** Added to `sites` in `src/db/schema.ts`: `openrouterMintingEnabled` (boolean, NOT NULL default false), `openrouterKeyHash` (text null), `openrouterMonthlyLimitUsd` (integer null). Reuses existing `openrouterApiKeyEncrypted` for the minted `sk-or-...`. Forward migration `migrations/0012_far_johnny_blaze.sql` (3 ALTER TABLE ADD). No behavior change. Gate: `tsc --noEmit` 0, re-`drizzle-kit generate` shows no drift, `npm test` 182/182, `npx opennextjs-cloudflare build` green.
- DONE (2026-06-23): **PM Edit Site UI — replace the paste field with minting controls.** `site-form.tsx`: removed the `sk-or-...` paste input + clear button; added a native-checkbox toggle (`openrouterMintingEnabled`), a `type=number` monthly USD limit (`openrouterMonthlyLimitUsd`, shown only when minting on), and a DISABLED "Delete current key" stub + "minted" hint shown only when `hasMintedOpenrouterKey` (live DELETE = Slice 5). New props `hasMintedOpenrouterKey`/`initialMintingEnabled`/`initialMonthlyLimitUsd` replace `hasOpenrouterKey`. PATCH sends toggle+limit (no key). New pure `parseOpenrouterMinting` (`openrouter-minting.ts`, `=== true` toggle, floored ≥0 int or null) wired into `parseSiteBody`; PATCH route persists via `updateSite`; key-encryption logic removed. Deleted dead `openrouter-key.ts`+test. i18n EN/FI/ET swapped. Gate: tsc 0, `npm test` 183/183, build GREEN.
- DONE (2026-06-23): **PM mint-on-deploy (idempotent) + delete endpoint.** Deploy route mints BEFORE the Slice-3 decrypt-thread block, gated on pure `shouldMintOnDeploy(enabled, keyHash)` (`src/lib/site/mint-on-deploy.ts`): mint only when enabled AND no `openrouterKeyHash` yet. `mintKey(provKey,{name:slug,limit:limitUsd??undefined})` → `encryptSecret` → `setSiteMintedOpenrouterKey(id,ct,hash)` (new site.ts helper, writes ciphertext+hash); local `site` mutated so the decrypt path threads the minted key. Mint failure → catch+warn+proceed (global fallback, never 500). `DELETE /api/sites/[id]/openrouter-key`: authz = deploy's (country-reach OR assignment); `deleteKey` then `clearSiteMintedOpenrouterKey` — proceed-and-clear even on remote 404. Form "Delete current key" button wired (was disabled). Gate: mint-on-deploy.test.mjs 4/4, tsc 0, `npm test` 187/187, build GREEN.
  - ORIGINAL: In the PM deploy route, BEFORE building the deployer POST: if `openrouterMintingEnabled` AND `openrouterKeyHash` is null (no key yet), call `mintKey(env.OPENROUTER_PROVISIONING_KEY, { name: site.slug, limit: site.openrouterMonthlyLimitUsd })`, encrypt the returned `sk-or-...` into `openrouterApiKeyEncrypted`, store the `hash`. If a key already exists (`openrouterKeyHash` non-null) DO NOT mint again. Minting failure MUST NOT crash deploy — catch, warn, proceed with the global fallback (omit the key from the deploy POST), same graceful-degrade pattern as the old decrypt path. Add `DELETE /api/.../openrouter-key` (the UI's delete button): `deleteKey(provisioningKey, hash)` then null out `openrouterKeyHash` + `openrouterApiKeyEncrypted` (proceed-and-clear even if the remote delete 404s — key already gone). The existing Slice-3 path that decrypts `openrouterApiKeyEncrypted` and threads it into the deploy POST stays as-is — it now carries the minted key. Pure decision helper (mint? / skip? from enabled+hash) with a dep-free `.mjs` test. In the PM deploy route, BEFORE building the deployer POST: if `openrouterMintingEnabled` AND `openrouterKeyHash` is null (no key yet), call `mintKey(env.OPENROUTER_PROVISIONING_KEY, { name: site.slug, limit: site.openrouterMonthlyLimitUsd })`, encrypt the returned `sk-or-...` into `openrouterApiKeyEncrypted`, store the `hash`. If a key already exists (`openrouterKeyHash` non-null) DO NOT mint again. Minting failure MUST NOT crash deploy — catch, warn, proceed with the global fallback (omit the key from the deploy POST), same graceful-degrade pattern as the old decrypt path. Add `DELETE /api/.../openrouter-key` (the UI's delete button): `deleteKey(provisioningKey, hash)` then null out `openrouterKeyHash` + `openrouterApiKeyEncrypted` (proceed-and-clear even if the remote delete 404s — key already gone). The existing Slice-3 path that decrypts `openrouterApiKeyEncrypted` and threads it into the deploy POST stays as-is — it now carries the minted key. Pure decision helper (mint? / skip? from enabled+hash) with a dep-free `.mjs` test.
- DONE (2026-06-23): **CMS-local user-key override.** New CMS-side `lib/settings/openrouter-key.ts` (pure: validate sk-or-, normalize, `toOpenrouterUserKeyStatus`, `effectiveOpenrouterKey(user,env)`) + `db/openrouter-key-store.ts` (mirrors google-client-store: `site_settings` row `openrouter_user_key` = `{keyEnc}`, AES-GCM via secret-box, KEK=`CMS_AUTH_SECRET`, `getDecryptedOpenrouterUserKey` → null on bad/missing). `getAi()` reads the CMS-local key (try/catch→null) and threads `effectiveOpenrouterKey(userKey, env.OPENROUTER_API_KEY)` into `pickSelection` — precedence CMS-local → env (minted/global) → CfAi → 503. REST `/api/settings/openrouter-key` (GET{hasKey}/PATCH{key}/DELETE, `requireUserManager`, key never echoed) + write-only UI + settings-nav tab + i18n EN/FI/ET. `ai.ts` imports its new deps RELATIVELY (not `@/`) so `.mjs` tests resolve. Gate: `openrouter-user-key.test.mjs` 9/9, tsc 0, CMS `npm test` 776/776, build GREEN.
- DONE (offline-complete 2026-06-23; live mint/delete/precedence = HITL): **Verify minting end-to-end.** All four codeable gates green: PM build ✅ + CMS build ✅ (dev off) + CMS suite 776/776 + PM suite 187/187. The KEY-MINTING TRACK is fully built & unit-covered; only live OpenRouter calls remain (need `OPENROUTER_PROVISIONING_KEY` on PM + a real key in CMS settings) → recorded in root HITL.md. This goal is effectively COMPLETE. PM toggle on + spend limit → deploy mints a key (idempotent: second deploy does NOT re-mint), key reaches the CMS Worker secret, chat streams; delete button revokes it via the API and clears PM state; CMS-local user key overrides the minted key at request time. `npx opennextjs-cloudflare build` green for PM + CMS (dev OFF first); all new unit tests + both suites green. Record the live mint/delete + precedence checks in the journal (live OpenRouter calls are the only non-codeable bits).

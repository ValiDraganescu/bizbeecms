# Backlog — ai-attachments
Task states: TODO | DOING | DONE | BLOCKED.

## Bugs
(human-reported bugs land here, newest at top; they outrank everything)

## Tasks
Tracer-first: prove modality gating + inline content-part shape in pure helpers, then wire the UI (picker + drop-zone reusing the existing uploader), then thread attachments into the chat request.

- DONE: **Tracer — pure attachment helpers (modality gate + inline content part).** Add a dep-free module (e.g. `CMS/src/lib/chat/attachments.ts`): `acceptsFile(modelInputModalities: string[], mime: string)` → maps a MIME to a modality (`image/* → image`, `application/pdf` + docs → file, `audio/* → audio`, `video/* → video`) and returns whether the selected model accepts it; `toInlineContentPart(mime, base64, name)` → the OpenAI-compatible content part (image → `{type:"image_url", image_url:{url:"data:<mime>;base64,..."}}`; file → the file content-part shape OpenRouter accepts). Node test: image accepted by a vision model, rejected by a text-only model; pdf gating; data-URI assembled correctly. NO UI yet. (Input modalities come from `ai-openrouter`'s catalog `architecture.input_modalities`; coordinate the model type — it should already carry `inputModalities` once that task lands, else read it here.)
- DONE: **`+` file picker + drag-and-drop on the chat textarea, gated to the selected model.** In `CMS/src/components/chat/chat-conversation.tsx` (the input area; coordinate with the textarea task in `ai-openrouter` if it lands first): add a `+` button (hidden `<input type=file multiple>`) AND drag-and-drop over the textarea. On drop/pick, run `acceptsFile` against the SELECTED model's `inputModalities` — unsupported → localized rejection toast/inline error, no attach. Accepted files upload via the EXISTING `POST /api/assets` (reuse `media-gallery.tsx`'s upload call; respect `MAX_ASSET_SIZE`) and render as removable chips on the pending message. Multiple files allowed. Disable the `+`/drop affordance (with a hint) when the model accepts only text. EN/FI/ET. Gate as usual.
- DONE: **Thread attachments into the chat request as inline base64.** `useChat.send(text, attachments)` now fetches each pending attachment's R2 bytes (`/media/<key>`), base64-encodes them, and builds the user message `content` as an OpenAI/OpenRouter ARRAY (`buildUserContent` → text part + one `toInlineContentPart` per file) fed through `buildModelHistory`. Widened `content` to `string | ContentPart[]` end-to-end (sse.ts `parseChatBody` validates user content-arrays; build-history, ai port, reframe). Adapter passes messages verbatim, so the array survives upstream. Transcript bubble stays plain text (📎 name lines). Node-tested + tsc + build + cms-bundle regen all green.
- DONE (codeable part): **Verify end-to-end.** All offline verification passes (2026-06-26): tsc clean, `npm test` 943/943, `npx opennextjs-cloudflare build` green (dev OFF; first run hit the stale-`.next` corruption gotcha — `rm -rf .next .open-next` then rebuild = clean), cms-bundle regenerated. Behaviors confirmed wired in source: `+` picker + drag-drop + `acceptsFile` gate in `chat-conversation.tsx`; `chat-widget.tsx` feeds the selected model's `inputModalities`; `blobToBase64`→`buildUserContent`→`buildModelHistory` threading; `sse.ts` `parseContentParts` validates user content-arrays; EN/FI/ET `chat.attach.*` keys all present & identical. **Still genuinely non-codeable** (needs a DEPLOYED Site CMS + a KEYED OpenRouter vision model): the live image round-trip — pick a vision model, attach an image, confirm the model RESPONDS about it; confirm a text-only model blocks non-text; confirm multiple-files. This requires a release/deploy the release manager owns + a vision-capable key; cannot be done from this repo alone. Codeable work for this goal is DRAINED.

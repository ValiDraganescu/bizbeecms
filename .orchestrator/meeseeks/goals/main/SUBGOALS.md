# Subgoals
Children that decompose main/GOAL.md. Each is a directory `goals/<slug>/` with its own GOAL.md + memory.
Status: ACTIVE (being worked / available to drive) | PAUSED (set aside by the user) | ARCHIVED (work folded into main, files moved to goals/archive/<slug>/). Goals never end — there is no DONE; ARCHIVED means "delivered, parked for reference".

## Active
- seo-robots — published-site SEO + crawler surface: sitemap audit + IndexNow, JSON-LD component kind + auto breadcrumbs, per-site robots.txt, 301 redirects w/ auto-capture on rename, per-page noindex, full OG/Twitter cards, branded per-locale 404 page, verification tokens, llms.txt + .md page variants, CWV image work (lazy/CLS post-pass + variant investigation), admin SEO-audit report, AI bulk-meta tool, OG-image autogen (Browser Rendering page-top screenshot fallback), per-IP bot rate limiting (created 2026-07-07) — ACTIVE
- ai-context-engineering — right-size the CMS AI assistant's context (user directive 2026-07-03): stale-thread (>24h) history compaction only — never mutate live threads (prompt-cache); paging on all list tools; context-aware buildSystemPrompt (drop out-of-scope sections); new data-sources admin context; dedup triple-paid binding prose + cap unbounded prompt lists — ACTIVE

## Archived (delivered; moved to goals/archive/<slug>/ — 2026-06-21)
- path-locales-edge-cache → `archive/path-locales-edge-cache/` — published pages: locale-prefixed URLs (default unprefixed), Stage 2 localized slugs w/ reverse-resolved links/switcher/hreflang/sitemap, Workers Cache edge caching (per-page opt-in, custom worker.ts Cache-Control/Cache-Tag stamping, publish + global-blast purge wiring), (site)/(admin) root-layout split killing Accept-Language cache poisoning. 21/21 tasks DONE incl. 3 real defect fixes; tests 1569→1688. OPEN: HITL-only — live cf-cache-status hit/miss/purge + lang fix on a DEPLOYED site (needs next r-* release); live AI create_page smoke — ARCHIVED → goals/archive/path-locales-edge-cache/ (2026-07-07)
- binding-adapters → `archive/binding-adapters/` — ports-and-adapters seam over CMS env.DB/MEDIA/AI + CF adapter (CF-native, no Vercel adapter). Db/Storage/Ai interfaces + CF adapters + mocked-port test shipped; the AI-over-REST path was delivered by ai-assistant.
- deploy-audit-trail → `archive/deploy-audit-trail/` — per-step deploy audit trail (events table + ingest API + localized timeline UI w/ per-step start/duration/error + run totals). Live end-to-end deploy spot-check is the only non-codeable check left.
- custom-domains → `archive/custom-domains/` — custom-domain SSO allowlist + always-visible DNS setup guide; `*.site.bizbeecms.com` scheme dropped (USER DECISION 2026-06-19) — sites stay permanently on `bizbeecms-cms-<slug>.workers.dev`.
- page-builder → `archive/page-builder/` — visual CMS page builder shipped: top bar + 3-col shell, layers/preview, block/page/SEO tabs, responsive columns, per-locale SEO + OG image, versioning (draft/publish/history/restore), AI-translate.
- ai-assistant → `archive/ai-assistant/` — page-aware Intercom-style CMS AI widget: per-page prompt + scoped tools, read/write tools, debug view, searchable model picker over the full Workers-AI catalog, per-Site history, multi-turn tool loop.
- pm-roles → `archive/pm-roles/` — PM user-management overhaul: 4-role hierarchy (SuperAdmin/Admin/Manager/Editor) w/ removal rules, Manager country+tag scope, global user-management UI+API — ARCHIVED → goals/archive/pm-roles/ (2026-06-23)
- page-builder-ux → `archive/page-builder-ux/` — CMS page-builder shell UX polish: resizable right inspector with 3 preset widths (default/¼/½) persisted, plus collapsible left rail + right inspector (each collapses to a thin re-expand strip, freeing the grid track to the canvas). Pure client + localStorage prefs (panel-size.ts, panel-collapse.ts). Was the live home for builder-UX polish; new builder-UX tweaks need a fresh live goal — ARCHIVED → goals/archive/page-builder-ux/ (2026-06-26)
- component-kits → `archive/component-kits/` — CMS custom-component tagging (free-form tags column + autocomplete + filter-by-tag in the components admin UI) and export-by-tag → one `*.kit.json` bundle (`format:"bizbeecms.kit"`), importable via the existing kit-install trust boundary; tags survive the round-trip in the portable envelope. Reused portable.ts + kit-install machinery, no managed tag table — ARCHIVED → goals/archive/component-kits/ (2026-06-26)
- ai-attachments → `archive/ai-attachments/` — CMS assistant file attachments: `+` picker + drag-drop drop-zone on the chat input, gated to the selected model's `inputModalities` (`acceptsFile`), uploaded to the existing per-site R2 `MEDIA` bucket via `/api/assets`, sent inline as base64 data-URIs to OpenRouter (`buildUserContent` → content-part array; wire widened to `string | ContentPart[]` through sse/build-history/Ai port). Input attachments only. HITL: live vision round-trip on a deployed CMS + keyed vision model — ARCHIVED → goals/archive/ai-attachments/ (2026-06-26)
- cms-mcp → `archive/cms-mcp/` — remote MCP server on each per-Site CMS Worker (`/mcp`) exposing the shared AI-tool registry, per-site bearer API-key auth (hashed at rest) minted in the CMS admin UI, second guard path alongside cms-auth. ALSO shipped the shared deployer `APP_ORIGIN` = primary-custom-domain fix (the dependency sso + auth-reset were waiting on — deployer/src/origin-core.ts + PM deploy route). DEFERRED (not greenlit): scoped/least-privilege per-key tool scopes. HITL: live MCP call against a deployed Worker + minted key; live custom-domain origin verify — ARCHIVED → goals/archive/cms-mcp/ (2026-06-26)
- cms-releases → `archive/cms-releases/` — CMS release + versioned-deploy system: repo `cms-release` skill (draft notes from commits → edit → semver tag `cms-v*` → bump + push), PM CMS-version PICKER (tagged releases only) + release-notes viewer on deploy, deployer `ref` wiring, `deployedCmsVersion` shown in PM site list + detail. HITL: live-verify after a `cms-v*` tag is pushed to the remote AND a site deployed from a chosen tag (`cms-v0.6.0` was local-only) — ARCHIVED → goals/archive/cms-releases/ (2026-06-26)
- content-collections → `archive/content-collections/` — user/AI-defined data collections: ONE real D1 table per collection via FENCED runtime DDL (content_* namespace, 100-table cap), typed schema, structured SQL query, rich UI + structured AI tools; Phase 2 component↔collection BINDING via built-in `List` block shipped. DEFERRED (not greenlit, re-file into a live goal to resume): Phase 3 route-driven detail pages + cross-collection, FTS5 full-text search — ARCHIVED → goals/archive/content-collections/ (2026-06-25)
- ai-openrouter → `archive/ai-openrouter/` — migrated CMS AI assistant onto OpenRouter behind the `Ai` port (swappable adapter): OpenRouterAi adapter + getAi() switch, OpenRouter model catalog (price /1M, tool-call filter, input-modality icons), per-Site key minting/precedence + deployer secret injection, minted-key credit display. Widget-UX tasks moved to ai-widget-ux. HITL: live `/api/v1/key` + per-Site secret on a deployed CMS — ARCHIVED → goals/archive/ai-openrouter/ (2026-06-25)
- cms-auth → `archive/cms-auth/` — CMS-local auth: in-CMS login page (email/password + Google OAuth + PM-SSO), CMS roles + invitations (Cloudflare Email), brute-force throttle, token prunes, secret-box KEK fix; `isPmSsoUser`/`ssoSyntheticEmail` live here (referenced by ai-widget-ux debug tasks) — ARCHIVED → goals/archive/cms-auth/ (2026-06-24)
- site-export-import → `archive/site-export-import/` — full-site export→import between CMS instances: operator REST + admin UI, destructive reset-then-restore import w/ dry-run report + typed confirmation, R2 assets, single-file zip export AND zip import (client-side fflate), scratch-instance.sh 2nd-local-CMS tooling; live-verified full cross-instance round-trip (61/61 assets, sha256-identical). Secrets travel as hasSecret flags only (re-entered on target) — ARCHIVED → goals/archive/site-export-import/ (2026-07-03)
- tableonline-home → `archive/tableonline-home/` — tableonline.fi replicated on local-site as Restovista: teal retheme, cities/offers/events/restaurants collections driving every home list, PLATFORM feature param-driven pages (`:name` wildcard slugs + `{param}`/`{query}` refs, nested wildcards, zero-match→404), /search, city + restaurant + offer detail pages, booking form → draft content_bookings, visual replica passes (dark bar/hero/promo strip/teaser/app badges/gradient footer), static legal pages, 2 acceptance audits closed, AI prompt taught wildcard pages. One waived polish TODO (SEO meta on /terms|/privacy|/contact) + a Known-gaps list in NEXT.md — re-file into a live goal if wanted — ARCHIVED → goals/archive/tableonline-home/ (2026-07-03)
- auth-reset → `archive/auth-reset/` — self-serve "forgot password" for BOTH PM and CMS: password_reset tables, enumeration-safe /api/auth/forgot (always 200), single-use TOCTOU-safe /api/auth/reset (guarded usedAt update, fresh PBKDF2 hash, session kill), forgot/reset pages + login links EN/FI/ET, behavioral fake-D1 test harness (shared util, source-grep asserts replaced), domain-prefixed subjects on all 4 mails (PM+CMS × invite+reset), bundle regen. Custom-domain link-host bug closed after the cms-mcp deployer APP_ORIGIN fix + user live HITL verify 2026-07-03 — ARCHIVED → goals/archive/auth-reset/ (2026-07-03)
- ai-widget-ux → `archive/ai-widget-ux/` — CMS AI-widget UX polish shipped in full: pointer-drag resize handle + default⇄half preset toggle (persisted), 3-row textarea + Enter-mode switch, tool-call accordion cards (input/output blobs, show-more truncation) persisted in chat history, unread badge, persisted model pick, scroll-to-latest pill, focus-trap + Esc + focus-ring a11y, PM-SSO-only chat-payload export + system-prompt versions (D1-backed, per-session override). Both bugs (model-picker crash on stale catalog cache; one-way expand toggle) fixed + user-confirmed live 2026-07-03 — ARCHIVED → goals/archive/ai-widget-ux/ (2026-07-03)
- sso → `archive/sso/` — CMS single sign-on live end-to-end on deployed custom-domain sites: Google own-client (redirect_uri_mismatch P1 closed via cms-mcp's deployer APP_ORIGIN fix; start route live-probed sending the custom-domain redirect_uri) + PM-SSO nonce handshake (cms-sso → sso-callback → cms-validate); both flows user-confirmed working live 2026-07-03. Build itself shipped earlier in archived cms-auth — ARCHIVED → goals/archive/sso/ (2026-07-03)
- external-data-sources → `archive/external-data-sources/` — external APIs as bindable DATA SOURCES: data_source + saved-request tables w/ write-only encrypted secret (header/query/basic/oauth2), CENTRAL server-side fetch engine (retries, per-request cache + purge via version counters, redirect + SSRF + streaming 5MB-cap hardening), source-agnostic binds (collection|api) incl. List + `{placeholder}` param passing, /admin/data-sources UI + Test panel, AI tools (create/test/list, create_form/bind_form, get_data_sources_guide, inline chat context), visitor FORM block (api|opted-in-collection targets, dual native/fetch submit, draft-only items), httpbingo live fixture page. Multiple defect hunts came up dry; workers declared it saturated — ARCHIVED → goals/archive/external-data-sources/ (2026-07-03)

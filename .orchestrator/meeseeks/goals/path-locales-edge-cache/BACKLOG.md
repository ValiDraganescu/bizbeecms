# Backlog — path-locales-edge-cache
Task states: TODO | DOING | DONE | BLOCKED.

## Bugs
(human-reported bugs land here, newest at top; they outrank everything)

## Tasks

### Stage 1 — locale-prefixed routes (do first: kills the Vary: Cookie need before caching ships)
- DONE: Extract the `resolvePage` tree walk + slug/plan resolution out of `CMS/src/app/[[...slug]]/page.tsx` into a shared lib module (e.g. `src/lib/render/resolve-page.ts`) so the later custom-worker cache wrapper can reuse it; page.tsx becomes a thin caller; all existing tests pass.
- DONE: Locale-prefix routing: teach the `[[...slug]]` route (via a pure helper in `lib/render/slug.ts`, unit-tested) to peel a leading segment matching a configured NON-default content locale — default locale stays unprefixed, `/` and `/<code>` both resolve HOME_SLUG; `render-page.tsx` takes the active locale from the route instead of the `bb_content_locale` cookie; preview keeps its explicit locale selection.
- DONE: Guard: reject a top-level page slug equal to a configured locale code — validated on page save AND on content-locale settings save (AND the AI create_page tool); localized error messages in messages/{en,fi,et}.json. Server returns stable `code` fields; clients map them to t() keys.
- DONE: Rework the builtin LanguageSwitcher (`lib/render/plan-language-switcher.ts`) from cookie+reload to real navigation (navigate to the same path under the target locale prefix). `CONTENT_LOCALE_COOKIE` NOT deleted (decision): the admin preview iframe has no locale routes, so /preview/ keeps cookie+reload; published pages never touch the cookie.
- DONE: Locale-prefix internal links at plan time: pure post-pass (`lib/render/localize-links.ts`) over the finished ElementPlan in `planPage` — every internal `href` gets the active non-default locale prefix (skips /media,/api,/admin,/preview,/_next, external//protocol-relative/#/mailto/relative, and already-locale-prefixed paths).
- DONE: SEO: emit `hreflang` alternate links + canonical in `generateMetadata` for every configured locale, and add a public `sitemap.ts` emitting all published pages × locales (no public sitemap exists today — only the admin view).

### Edge caching (Workers Cache; needs Stage 1 landed)
- DONE: Enable Workers Cache: `"cache": { "enabled": true }` in CMS/wrangler.jsonc with static-asset billing comment; cf-typegen re-run (no Env change — cache lives on ctx, not env); deploy-gate build + `wrangler deploy --dry-run` green.
- DONE: Drizzle migration 0027: `page.cache_max_age` INTEGER NOT NULL DEFAULT 0 (0 = never cache); optional `cacheMaxAge` in validatePageMeta (allowed set {0,300,3600,86400}, absent = preserve stored value); page-settings "Edge cache" select (Off / 5 min / 1 h / 1 day) via pure `buildCacheMaxAgeBody`; en/fi/et strings.
- DONE: Custom worker entrypoint (officially supported by OpenNext): new `CMS/worker.ts` set as wrangler `main`, importing the `.open-next/worker.js` handler; for GET 200 responses on public page paths (not /admin, /api, /media, /preview, /_next) without Set-Cookie, resolve the slug via the shared resolver and, when `cache_max_age > 0`, set `Cache-Control: public, max-age=<n>, stale-while-revalidate=86400` + `Cache-Tag: pages,page:<id>`. The extra D1 lookup only runs on cache misses. (Pure gate in lib/render/edge-cache.ts; resolve-page/load-plan split keeps Next out of the worker bundle; header stamping live-verified via wrangler dev.)
- DONE: Purge wiring: `/api/pages/[id]/publish` (and unpublish/delete paths) call `ctx.cache.purge({ tags: ["page:<id>"] })` via `getCloudflareContext().ctx`; theme colors save, theme fonts save, component publish, brand-identity save, and locale-settings save purge the shared `pages` tag. Best-effort: a purge failure must never fail the write.

### Stage 2 — localized slugs (`/fi/meista`)
- DONE: Localized-slugs data model: add `page.localized_slugs` JSON column (existing `slug` column stays the default-locale slug; `UNIQUE(parent_page_id, slug)` index intact); per-locale sibling-uniqueness validation on save; per-locale slug inputs in page settings (mirroring metaTitle's per-locale UI). (Migration 0028; pure localizedSlugSiblingConflicts + buildLocalizedSlugsBody; locale-code guard extended to localized values on both write paths.)
- TODO: Wire `localizedSlugSiblingConflicts` into the AI create_page path (`upsertPage` in page-store) — a new AI page's default slug can collide with a sibling's per-locale override; small, do alongside/after the locale-aware walk.
- DONE: Locale-aware slug walk: `matchSlugSegment`/`resolvePage` take the active locale and resolve against `effectiveSlug` = `localizedSlugs[locale] ?? slug` (override-only where set — the default slug 404s in that locale); wildcard `:param` slugs stay locale-agnostic; both callers (loadPlan route + worker.ts edge-cache stamping) pass the peeled locale; 9 dep-free unit tests + wrangler-dev live smoke.
- DONE: Reverse-resolve internal links + switcher under localized slugs (part 1): pure `localize-paths.ts` translator (default path → locale slug chain) + `pagePathsByLocale`; threaded via `LocaleContext.translatePath`/`pagePaths` from buildPlanFromPage; localizeHref translates before prefixing; switcher options carry plan-time `data-bb-path` (client falls back to prefix rewrite).
- DONE: Reverse-resolve hreflang + sitemap under localized slugs (part 2): `pathForLocale` takes optional `translate` (sitemap builds createPathTranslator from its own row read + localizedSlugs col); `hreflangAlternates` takes optional plan-time `pagePaths` (wins over prefix rewrite, per-code fallback); generateMetadata passes locale.pagePaths — zero new D1 reads on the metadata path.

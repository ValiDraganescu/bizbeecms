# Backlog — ai-widget-ux
Task states: TODO | DOING | DONE | BLOCKED.

## Bugs
(human-reported bugs land here, newest at top; they outrank everything)

## Tasks
Pure CMS chat-widget UX. Widget shell = `chat-widget.tsx`; transcript/input/tool-cards = `chat-conversation.tsx`. Each gates on CMS tsc + `npm test` + `npx opennextjs-cloudflare build` (dev OFF) + cms-bundle regen + EN/FI/ET for new strings.

- TODO: **In-chat system-prompt editor + versions — PM-SSO users ONLY (prompt iteration).** Let a PM-SSO operator edit the system prompt right in the chat UI and switch between saved versions to compare results. NOT pure client (needs server storage + the chat route) — exception to this goal's client-only rule, noted in CAVEATS. Behavior (USER 2026-06-24): a NEW version starts as a COPY of the current auto-assembled default (`assembleSystemPrompt` in `CMS/src/lib/chat/assemble-prompt.ts`) — so the editor must seed from that default (reuse the `GET /api/chat/debug` builder to fetch it) — then the operator edits it freely; the edited text IS the version (full prompt, not a layer). STORAGE: per-Site CMS D1 — new table (id, name/label, prompt text, createdAt) + migration + gated CRUD route (`/api/chat/prompts`), shared by all PM-SSO operators on the site. BLAST RADIUS: selecting a version applies to the TESTER's SESSION ONLY — the widget sends the chosen prompt text per-request (a `systemPromptOverride` field on the chat POST); the chat route, WHEN the override is present AND the caller is PM-SSO, uses it INSTEAD of `assembleSystemPrompt` for that request; real end-users + other operators keep the default. No site default is changed. UI: a versions dropdown + edit/save/new/delete, placed in/near the debug panel; visible only to PM-SSO. GATE everywhere (route CRUD + the chat-route override path): PM-SSO only (reuse the `isPmSsoUser(user)` predicate from the export task — build it once, share it) — 403 for non-SSO, and the override field is IGNORED for non-SSO callers (can't have a local user inject a prompt). Pure helpers: validate prompt input, and "effective system prompt = override-if-present-and-sso else assembled" decision — node-tested. EN/FI/ET for all new strings. Gate: CMS tsc + `npm test` + `npx opennextjs-cloudflare build` (dev OFF) + cms-bundle regen. (Depends on / shares `isPmSsoUser` with the export task — do that one first or build the predicate here.)

- DONE (2026-06-24): **Export chat as the full model-payload JSON — PM-SSO users ONLY (debug tool).** Pure `lib/auth/pm-sso.ts` `isPmSsoUser`/`isPmSsoEmail` (synthetic `@pm.sso` match; +test, 3 cases). Server guard `requirePmSso(request)` + `currentUserIsPmSso()` in `guard.ts` (401 not-signed-in → 403 non-SSO). New `POST /api/chat/export` (POST, not GET — transcript messages live client-side; re-assembles system prompt + messages + tool schemas + resolved model id, no model call). `GET /api/chat/debug` now returns `isPmSso` so the panel knows to show the button. `ChatDebugPanel` gained `messages`/`model` props + an "Export chat" button (PM-SSO only) that downloads `chat-payload-<context>.json`. Wired in `chat-widget.tsx`. EN/FI/ET `chat.debug.{export,exporting,exportError}`. Predicate built ONCE here for the system-prompt task to reuse.

- DONE (2026-06-24): **Resizable AI-assistant chat window + preset sizes.** Helper `lib/chat/panel-size.ts` (+test) + native CSS `resize` + default⇄half toggle in `chat-widget.tsx`, size persisted in localStorage `bizbee.chat.panelSize`, re-clamped to viewport. In `chat-widget.tsx`: let the user resize the open panel — a drag handle (CSS `resize` on the panel container is the lazy path if it fits the layout, else a corner-drag handler) AND a preset toggle offering the two sizes: the CURRENT default size and a HALF-SCREEN size (≈50vw/50vh — confirm exact against the layout). Persist the chosen size/preset across open/close + reloads (localStorage, keyed per-CMS is fine — it's a UI pref, not server state). Keep it within viewport bounds (min size so it can't vanish; clamp to not overflow). Reuse design-system tokens; no native UI that breaks browser-review. Gate as above (+ EN/FI/ET for any new control label).

- DONE (2026-06-24): **Chat input → resizable 3-row textarea + Enter-behavior switch.** `<input>` → `<textarea rows={3}>` (CSS `resize-y`, min 5.5rem/max 16rem) in `chat-conversation.tsx`. Enter-mode toggle "↵ Send"⇄"↵ Newline"; pure `lib/chat/enter-mode.ts` `decideSendOnEnter(mode,{shift,meta,ctrl})` + localStorage `bizbee.chat.enterMode` (+7-case node test). EN/FI/ET `chat.enterMode.{send,newline,aria}`. Default = Enter-to-send.

- DONE (2026-06-24): **Tool-call cards: stop repeating the name + add an input/output accordion.** Pure `lib/chat/tool-card.ts` (`toolSubject`/`toolSummary`/`formatBlob` +8 tests). `ToolResult` (client-sse) gained `input?`/`output?`; `route.ts runToolsRound` frames `{...data, input:call.args}`. `ToolCard` is now a native `<details>` accordion: name once + summary + fail badge in `<summary>`, Input/Output `<pre>` blobs on expand. Removed `chat.tool.ok`/`fail`; added `chat.tool.{failBadge,input,output}` EN/FI/ET. Original task text below for reference. In `CMS/src/components/chat/chat-conversation.tsx` the `ToolCard` (~line 294) renders `t("tool.ok", { name, action, subject })` which currently shows the tool name TWICE (e.g. "get_brand_identity: get_brand_identity"). (1) Fix the label so the name appears ONCE (clean up the `tool.ok`/`tool.err` i18n strings + the `subject = component ?? page ?? target ?? name` fallback that duplicates when subject===name). (2) Make each card a collapsible ACCORDION: collapsed by default showing just the one-line label + ok/err state; expandable to reveal the tool-call INPUT (args) and OUTPUT (result). This requires the `ToolResult` type (~line 24) + the streamed `tool` event (`addTool`, ~line 77/119) and the chat route's SSE to carry the call args + raw result (today the card only has name/action/subject/ok) — thread those through. Render input/output as readable pre/JSON (truncate huge blobs with a "show more"; ponytail: simple `<details>`/`<summary>` is the lazy accordion if it fits the design system). EN/FI/ET for the expand/collapse + "input"/"output" labels.

- DONE (2026-06-24): **Persist tool calls in chat history (survive refresh / thread reload).** No migration — tools live inside the existing `chat_thread.messages` JSON blob. `lib/chat/history.ts` `ThreadMessage` gained optional `tools?: StoredTool[]` + exported `sanitizeTools` (plain-object-only, JSON-roundtrip, cap 50, assistant-only); `validateThreadInput`/`parseStoredMessages` round-trip them. `chat-widget.tsx` save payload + `openThread` carry tools; `chat-conversation.tsx seed()` restores them (was `[]`). Stored = enriched input/output shape so reloaded cards expand. 6-case `history.test.ts`.

- DONE (2026-06-24): **Unread badge on the assistant icon when minimized + a new reply arrives.** Pure `nextUnread(current, {open, replyFinished})` in `lib/chat/unread-badge.ts` (+5-case node test). `chat-widget.tsx`: `unread` state set on the busy→idle finish edge when `!open` (reused the save effect's edge + added `open` to its deps), cleared by an open→clear effect; danger dot rendered on the launcher when `unread && !open` with `chat.widget.unread` aria/title (EN/FI/ET). Dot, no count/sound.

- DONE (2026-06-24): **Persist the selected model across reloads.** Pure `resolveInitialModel(stored, catalogIds, fallback)` + `loadModel`/`saveModel` in `lib/chat/selected-model.ts` (+5-case test, localStorage `bizbee.chat.model`). `chat-widget.tsx`: `setModel` writes through; mount effect fetches `/api/chat/models`, validates the stored id against live catalog ids, falls back to default if removed. No new i18n.

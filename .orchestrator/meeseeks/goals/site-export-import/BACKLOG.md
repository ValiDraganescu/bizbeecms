# Backlog — site-export-import
Task states: TODO | DOING | DONE | BLOCKED.

## Bugs
(human-reported bugs land here, newest at top; they outrank everything)

## Tasks
- DONE: Design + inventory slice: read GOAL.md, then (a) enumerate EXACTLY what each exported table contains (schema.ts + the stores) and which site_settings keys exist; (b) mine goals/archive/component-kits (portable.ts envelope + kit-install trust boundary) and goals/archive/content-collections (fenced DDL); (c) write the artifact format spec into this goal dir as FORMAT.md — envelope (`format:"bizbeecms.site"`, formatVersion, cmsVersion, counts, checksums?), how collections rows are encoded, how assets are referenced, size strategy (single zip via fflate vs manifest+per-asset protocol — pick one and justify), and the import reset plan (exact tables wiped vs preserved). No product code this run; FORMAT.md is the deliverable and every later task's contract.
- DONE: Export core (tracer, no assets yet): operator-only REST endpoint (e.g. GET /api/site-export) that produces the site.json per FORMAT.md — pages (+ current draft/live versions), components, collection schemas + all content_* rows, site_settings, prompt_version, data_source + requests (hasSecret flags, NO secret values), asset TABLE rows (binaries later). Pure serialization logic unit-tested; commit.
- DONE: Export assets: extend the export to include R2 binaries per FORMAT.md's chosen strategy (zip stream or per-asset fetch protocol) so gallery images travel with the artifact. Verify a real image round-trips byte-identical locally.
- DONE: Import validate + dry-run: operator-only endpoint that accepts the artifact, validates envelope/format/version, and returns a dry-run report (counts to be created, what will be DESTROYED on the target, which data sources will need secret re-entry, collection-count vs 100-table cap check). No writes yet. Unit-test the validator + report builder.
- TODO: Import execute: the destructive path per FORMAT.md — wipe content/design tables + drop content_* tables (preserve user/session/invite/password_reset/login_attempt/api_key/icon_cache/chat_thread), recreate collections via the fenced DDL path, insert rows, restore settings/pages/components/prompts/data-sources, upload assets to R2. Idempotent-ish: a failed import must be re-runnable. Unit-test the reset planner + row restorer with mocked ports; live-verify on :3602 with a small export.
- TODO: Admin UI: Settings → "Export / Import" section — Export button (downloads the artifact), Import flow (upload → dry-run report rendered → typed confirmation e.g. site name → execute → result report incl. secrets-to-re-enter list). i18n via next-intl like sibling admin pages.
- TODO: E2E/HITL slice: export the local-site (:3602) with its full tableonline content, import it into a scratch second instance (second local D1 database or the deployed bizbeecms-cms-test-1 — pick the cheaper one), and click through: home renders identically, a city page, a booking form submit, gallery images load. Record gaps as new TODOs.

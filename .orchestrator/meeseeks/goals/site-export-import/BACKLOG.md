# Backlog — site-export-import
Task states: TODO | DOING | DONE | BLOCKED.

## Bugs
(human-reported bugs land here, newest at top; they outrank everything)

## Tasks
- DONE: Design + inventory slice: read GOAL.md, then (a) enumerate EXACTLY what each exported table contains (schema.ts + the stores) and which site_settings keys exist; (b) mine goals/archive/component-kits (portable.ts envelope + kit-install trust boundary) and goals/archive/content-collections (fenced DDL); (c) write the artifact format spec into this goal dir as FORMAT.md — envelope (`format:"bizbeecms.site"`, formatVersion, cmsVersion, counts, checksums?), how collections rows are encoded, how assets are referenced, size strategy (single zip via fflate vs manifest+per-asset protocol — pick one and justify), and the import reset plan (exact tables wiped vs preserved). No product code this run; FORMAT.md is the deliverable and every later task's contract.
- DONE: Export core (tracer, no assets yet): operator-only REST endpoint (e.g. GET /api/site-export) that produces the site.json per FORMAT.md — pages (+ current draft/live versions), components, collection schemas + all content_* rows, site_settings, prompt_version, data_source + requests (hasSecret flags, NO secret values), asset TABLE rows (binaries later). Pure serialization logic unit-tested; commit.
- DONE: Export assets: extend the export to include R2 binaries per FORMAT.md's chosen strategy (zip stream or per-asset fetch protocol) so gallery images travel with the artifact. Verify a real image round-trips byte-identical locally.
- DONE: Import validate + dry-run: operator-only endpoint that accepts the artifact, validates envelope/format/version, and returns a dry-run report (counts to be created, what will be DESTROYED on the target, which data sources will need secret re-entry, collection-count vs 100-table cap check). No writes yet. Unit-test the validator + report builder.
- DONE: Import execute: the destructive path per FORMAT.md — wipe content/design tables + drop content_* tables (preserve user/session/invite/password_reset/login_attempt/api_key/icon_cache/chat_thread), recreate collections via the fenced DDL path, insert rows, restore settings/pages/components/prompts/data-sources, upload assets to R2. Idempotent-ish: a failed import must be re-runnable. Unit-test the reset planner + row restorer with mocked ports; live-verify on :3602 with a small export.
- DONE: Asset bytes upload leg: POST /api/site-import/asset/<key> (requireAdmin, Storage.put, mirrors export asset route + isValidAssetKey traversal guard). Content-type read from the already-restored asset row, not trusted from the client.
- DONE: Admin UI: Settings → "Export / Import" section — Export button (downloads the artifact), Import flow (upload → dry-run report rendered → typed confirmation e.g. site name → execute → result report incl. secrets-to-re-enter list). i18n via next-intl like sibling admin pages.
- DONE: E2E/HITL slice: export the local-site (:3602) with its full tableonline content, import it into a scratch second instance (second local D1 database or the deployed bizbeecms-cms-test-1 — pick the cheaper one), and click through: home renders identically, a city page, a booking form submit, gallery images load. Record gaps as new TODOs. Found + FIXED 2 real cross-instance bugs (see JOURNAL) that every prior same-instance smoke test couldn't have caught.

## New TODOs found by the E2E slice (2026-07-02)
- TODO: `readSiteName`/`checkConfirmation` gap — a site with a NON-empty `site_identity.brandName` but where the operator's confirm-typing UI (or a scripted import) sends `confirm` with different whitespace/casing than the export snapshot will hard-refuse; not a bug, just worth a UX nit: the admin UI's confirm field could show the exact expected string more prominently (currently it does show `artifact.meta.siteName` in a `<strong>`, so this is LOW priority / maybe already fine — re-check the UI copy if an operator ever reports confusion).
- DONE (2026-07-02): Wipe-loop atomicity — `POST /api/site-import`'s `dropContentTables` loop now runs as ONE `contentDdlBatch` call (D1's implicit batch transaction) instead of one `contentDdl` per table, so a transient D1 error mid-loop can no longer leave a partial DROPPED/not-DROPPED state. `contentDdlBatch` already existed (built for the schema-rebuild path) — reused verbatim, no new primitive. Live-verified on the real dev D1: full same-instance export→import round-trip (13 pages/136 versions/41 components/7 collections/73 rows/61 assets/6 data sources/12 requests/2 prompt versions, all matched), home + a city page both 200 after. `npm test` 1501/1501, `tsc --noEmit` clean.
- TODO: `next.config.ts`'s `initOpenNextCloudflareForDev()` has no supported way to point at an alternate `.wrangler` persist dir short of running from a genuinely separate directory (Turbopack refuses ANY symlinked path outside its project root — `node_modules`, `src`, everything). If a second local instance becomes a recurring need (not just one-off E2E verification), consider either (a) a small script that spins up a real scratch dir via `rsync`/copy + port + `.dev.vars` automatically, or (b) investigating `wrangler dev` (not `next dev`) with `--persist-to` for a lighter-weight second D1/R2 without a second full Next.js process. Not done this run — out of scope, the manual copy approach was sufficient for one E2E pass.

# Backlog — tableonline-home
Task states: TODO | DOING | DONE | BLOCKED.

## Bugs
(human-reported bugs land here, newest at top; they outrank everything)

## Tasks
- DONE: Retheme the local site to the tableonline palette via the `update_theme` MCP tool — light: surface #f9f9f6, foreground #001414, primary #124142, primary-hover #073535 (or darker), plus coherent subtle/border/ring tokens; dark mode teal-tinted. Verify the home page on :3602 visually picks it up (curl the HTML / check computed CSS vars).
- DONE: Create a `cities` collection (title, slug, country_code, image) via MCP, seed ~11 FI+EE cities (see GOAL.md), generate/reuse images, then make the home CityLinks section collection-driven — either bind a List with a city photo-card template component (photo + dark bottom gradient + white name, links to /{slug}) or update the CityLinks component to bind. Keep the Finland/Estonia tab split via country_code filter.
- DONE: Create an `offers` collection (title, slug, image, description, date_range, restaurant, city, is_main) via MCP, seed it from the 4 hardcoded OfferCard props on the home page + 2-4 more, then replace the hardcoded offers row with a List bound to the collection (OfferCard as template, listMap the fields). Each card links to the offer detail page path (/offers/{slug} — page may not exist yet; that's a later task).
- DONE: Create an `events` collection (title, slug, image, description, date, restaurant, location, rating) via MCP, seed from the 4 hardcoded EventCard props + a few more, replace the hardcoded events row with a bound List. (Audit 2026-07-02 confirmed: `content_events` does not exist yet and the 4 EventCard blocks are still hardcoded — this is the last non-collection list on the page.)
- TODO: Data-fix `content_restaurants` so items can route (audit 2026-07-02): most of the 26 restaurants have empty/null `slug` — fill kebab-case slugs for ALL; add a `city_slug` field (or verify `location` maps cleanly onto content_cities slugs) so city pages can filter; generate images for the ~5 items with `image: null` (Maxill, BasBas Kulma, Aperte, Plein, The Bull & The Firm); replace every external `book_href` (they leak to https://www.tableonline.fi and example.com placeholders!) with the internal pattern `/book?restaurant={slug}` (will 404 until the book page exists — acceptable, it stops the off-site leak).
- TODO: Platform feature — dynamic/param-driven pages: support a page whose slug contains a wildcard segment (e.g. `city/:slug` or `/:city` catch-all) where the param is available to blocks as a binding/filter value, AND make URL query params (e.g. ?q=) available the same way. READ `goals/archive/content-collections/` first — "Phase 3 route-driven detail pages" was deferred there and is this exact feature. This is repo-code work in CMS/; commit it like any CMS feature with tests.
- TODO: Build the search page at /search using the query-param feature. tableonline reference (live-verified): params are `q` (free text) + optional `city`, `date`, `period` (lunch|dinner), `availability`, `sort`, `order` — we only need `q` (+ `city` if cheap). Layout: breadcrumb + h1 "Results for '{q}'", result ROWS (thumbnail, name link, city, rating x.xx/5 + review count, Book pill) not cards, "Showing N restaurants" count, empty state "No results found — adjust your search". Filter on content_restaurants name/location contains q, case-insensitive. ALSO make the HomeHero search a real form: input currently has no name and no <form> — it must submit GET to /search with name="q" (header search too, if trivial).
- TODO: Build the city landing page using the wildcard-page feature: /{city-slug} (or /city/{slug} if root wildcards clash). tableonline reference (live-verified): breadcrumb "Restaurants » {City}", h1 city name over dark hero, then "Best restaurants in {city}" 4-col card grid (filter content_restaurants by city), "Offers in {city}" grid (content_offers filter city), "Upcoming events in {city}" grid, "Newest restaurants in {city}" ROW list, each strip with a "Show all" link to /search?city=... (or the closest we support). Quick-filter pills under the hero are optional polish. Wire the home city cards to it.
- TODO: Build the offer detail page (/offers/{slug} via the wildcard feature): image, title, date range, description, restaurant, and a "Book a table" CTA linking to the book page with the restaurant preselected. Wire home OfferCards to it (they already link /offers/{slug}).
- TODO: Build the book-a-table page reached from "Book" CTAs and the offer page, pre-selecting the restaurant via param. tableonline reference: two-step single page — step 1: party-size dropdown, date, time-slot pill grid grouped by Lunch/Dinner; step 2 (inline after slot pick): first/last name, email, phone, message, terms-agree checkbox, "Confirm and Book". Ours can be one form (restaurant, date, time, party size, name, email, message + consent checkbox) via the CMS form machinery (create_form/bind_form MCP tools) into a bookings collection; keep the two-step look only if cheap.
- TODO: Build the restaurant detail page /{city-slug}/{restaurant-slug} via the wildcard feature (restaurant names on home/city/search cards should link here, not straight to book). tableonline reference: breadcrumb, h1 + address + cuisine tag, image, description, 4 rating badges in a row (Overall/Kitchen/Atmosphere/Service — we have only `rating`, so render Overall + review count and skip the rest or fake sub-scores), tag pills, "Book a table" CTA to the book page. Keep it one page, no tabs.
- TODO: Home page visual replica pass 1 (header/hero): dark top utility bar (#14151a — audit found ours is light bg-surface, inverted contrast; fix to dark) with "For restaurants" + language switcher, transparent nav over the full-bleed photo hero with 50% dark overlay + white serif headline; promotions strip that rides up over the hero bottom (-4rem) with one large 16/9 main promo (content_offers where is_main=1 — field exists, currently unused; set it on 1-2 seeded offers) + secondary promo column. The existing 2 PromoBanner blocks at page bottom are a different thing — leave them (tableonline has the same Avios/newsletter tiles near the footer).
- TODO: Home page visual replica pass 2 (below the fold): registration teaser callout (1px #124142 border, faint teal bg, perks + CTA — absent today); app download section (App Store / Google Play badges — absent today); footer upgrade: vertical gradient #001414→#073535 (currently flat bg-foreground), social icon row ("Follow us"), copyright bar, and replace the 9 dead href="#" footer links with real targets where pages exist (/{fi|en} home, /search, city pages) — point not-yet-built legal pages at /about for now and note it.
- TODO: Static/footer pages so no footer link is dead: simple content pages for Terms, Privacy policy, Contact us, and "For restaurants" (reuse RestaurateurJoin content) + update footer links to them. Low priority — do after all landing pages above ship.

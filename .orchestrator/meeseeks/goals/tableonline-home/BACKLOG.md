# Backlog — tableonline-home
Task states: TODO | DOING | DONE | BLOCKED.

## Bugs
(human-reported bugs land here, newest at top; they outrank everything)

## Tasks
- DONE: Retheme the local site to the tableonline palette via the `update_theme` MCP tool — light: surface #f9f9f6, foreground #001414, primary #124142, primary-hover #073535 (or darker), plus coherent subtle/border/ring tokens; dark mode teal-tinted. Verify the home page on :3602 visually picks it up (curl the HTML / check computed CSS vars).
- DONE: Create a `cities` collection (title, slug, country_code, image) via MCP, seed ~11 FI+EE cities (see GOAL.md), generate/reuse images, then make the home CityLinks section collection-driven — either bind a List with a city photo-card template component (photo + dark bottom gradient + white name, links to /{slug}) or update the CityLinks component to bind. Keep the Finland/Estonia tab split via country_code filter.
- DONE: Create an `offers` collection (title, slug, image, description, date_range, restaurant, city, is_main) via MCP, seed it from the 4 hardcoded OfferCard props on the home page + 2-4 more, then replace the hardcoded offers row with a List bound to the collection (OfferCard as template, listMap the fields). Each card links to the offer detail page path (/offers/{slug} — page may not exist yet; that's a later task).
- DONE: Create an `events` collection (title, slug, image, description, date, restaurant, location, rating) via MCP, seed from the 4 hardcoded EventCard props + a few more, replace the hardcoded events row with a bound List. (Audit 2026-07-02 confirmed: `content_events` does not exist yet and the 4 EventCard blocks are still hardcoded — this is the last non-collection list on the page.)
- DONE: Data-fix `content_restaurants` so items can route (audit 2026-07-02): most of the 26 restaurants have empty/null `slug` — fill kebab-case slugs for ALL; add a `city_slug` field (or verify `location` maps cleanly onto content_cities slugs) so city pages can filter; generate images for the ~5 items with `image: null` (Maxill, BasBas Kulma, Aperte, Plein, The Bull & The Firm); replace every external `book_href` (they leak to https://www.tableonline.fi and example.com placeholders!) with the internal pattern `/book?restaurant={slug}` (will 404 until the book page exists — acceptable, it stops the off-site leak). Actual audit found 21 (not ~5) null-image items; all fixed — see JOURNAL 2026-07-02 16:02.
- DONE: Platform feature — dynamic/param-driven pages: support a page whose slug contains a wildcard segment (e.g. `city/:slug` or `/:city` catch-all) where the param is available to blocks as a binding/filter value, AND make URL query params (e.g. ?q=) available the same way. READ `goals/archive/content-collections/` first — "Phase 3 route-driven detail pages" was deferred there and is this exact feature. This is repo-code work in CMS/; commit it like any CMS feature with tests. See JOURNAL 2026-07-02 16:15 for the design (`:name` wildcard slug segment + `{param:"x"}`/`{query:"x"}` filter-value refs, resolved per-request, graceful drop when absent) and the live end-to-end smoke test.
- DONE: Build the search page at /search using the query-param feature. tableonline reference (live-verified): params are `q` (free text) + optional `city`, `date`, `period` (lunch|dinner), `availability`, `sort`, `order` — we only need `q` (+ `city` if cheap). Layout: breadcrumb + h1 "Results for '{q}'", result ROWS (thumbnail, name link, city, rating x.xx/5 + review count, Book pill) not cards, "Showing N restaurants" count, empty state "No results found — adjust your search". Filter on content_restaurants name/location contains q, case-insensitive. ALSO make the HomeHero search a real form: input currently has no name and no <form> — it must submit GET to /search with name="q" (header search too, if trivial). Required a small platform extension (List `search` route-value support + prop-level route refs) — see JOURNAL 2026-07-02 16:31.
- DONE: Build the city landing page using the wildcard-page feature: /{city-slug} (or /city/{slug} if root wildcards clash). tableonline reference (live-verified): breadcrumb "Restaurants » {City}", h1 city name over dark hero, then "Best restaurants in {city}" 4-col card grid (filter content_restaurants by city), "Offers in {city}" grid (content_offers filter city), "Upcoming events in {city}" grid, "Newest restaurants in {city}" ROW list, each strip with a "Show all" link to /search?city=... (or the closest we support). Quick-filter pills under the hero are optional polish. Wire the home city cards to it. Shipped as page slug `:city-slug` w/ CityHero+CityStripHeading+CityEmptyStrip (new) + existing RestaurantCard/RestaurantRow/OfferCard/EventCard templates; "Show all" → /search (no ?city= filter yet). See JOURNAL 2026-07-02 16:42.
- DONE: Build the offer detail page (/offers/{slug} via the wildcard feature): image, title, date range, description, restaurant, and a "Book a table" CTA linking to the book page with the restaurant preselected. Wire home OfferCards to it (they already link /offers/{slug}). Shipped as a NESTED wildcard `:offer-slug` page under a new static `offers` index page (`parentSlug:"offers"`) — confirmed nested wildcards under a static parent work with zero new platform code. New `OfferHero` component + `content_offers.restaurant_slug` field (backfilled by slugifying the free-text `restaurant` name — offers/events reference restaurant names that don't exist as rows in content_restaurants). See JOURNAL 2026-07-02 16:50.
- DONE: Build the book-a-table page reached from "Book" CTAs and the offer page, pre-selecting the restaurant via param. Shipped as ONE single-step form (not tableonline's two-step pill-grid flow — noted as a visual simplification, not attempted) at a new top-level page `book`: `BookingForm` component (hidden restaurant_slug/restaurant_name echoing `?restaurant=` via prop-level `{"query":"restaurant"}` refs, date, time, party-size select, name, email, message, required consent checkbox) wired via `create_form` into a new `bookings` collection (`publicSubmissions:true`, enabled via the operator-only REST PATCH). Full round-trip verified: form renders, submits, lands as a draft `content_bookings` row with every field intact. See JOURNAL 2026-07-02 16:57.
- DONE: Build the restaurant detail page /{city-slug}/{restaurant-slug} via the wildcard feature (restaurant names on home/city/search cards should link here, not straight to book). tableonline reference: breadcrumb, h1 + address + cuisine tag, image, description, 4 rating badges in a row (Overall/Kitchen/Atmosphere/Service — we have only `rating`, so render Overall + review count and skip the rest or fake sub-scores), tag pills, "Book a table" CTA to the book page. Keep it one page, no tabs. Shipped as `:restaurant-slug` nested under `:city-slug` (first wildcard-under-wildcard page this goal built — confirmed working with zero platform changes once published, see JOURNAL). Also repointed RestaurantRow/RestaurantCard/SearchResultRow name-links from `/book?restaurant=…` to `/{citySlug}/{slug}`.
- DONE: Home page visual replica pass 1 (header/hero): dark top utility bar (#14151a — audit found ours is light bg-surface, inverted contrast; fix to dark) with "For restaurants" + language switcher, transparent nav over the full-bleed photo hero with 50% dark overlay + white serif headline; promotions strip that rides up over the hero bottom (-4rem) with one large 16/9 main promo (content_offers where is_main=1 — field exists, currently unused; set it on 1-2 seeded offers) + secondary promo column. The existing 2 PromoBanner blocks at page bottom are a different thing — leave them (tableonline has the same Avios/newsletter tiles near the footer). Shipped: SiteHeader now has a hardcoded `#14151a` utility bar + 5 new class-override props (only home's instance overrides them to transparent/white + `position:absolute` via a new `wrapClass` prop — every other page keeps the old opaque look, zero regression); HomeHero overlay flattened to `bg-foreground/50`; new `PromoMain`/`PromoSecondary` components in a new 2-column Section with column-level `marginTop:-4rem` riding it up over the hero. See JOURNAL 2026-07-02 17:16.
- DONE: Home page visual replica pass 2 (below the fold): registration teaser callout (1px #124142 border, faint teal bg, perks + CTA — absent today); app download section (App Store / Google Play badges — absent today); footer upgrade: vertical gradient #001414→#073535 (currently flat bg-foreground), social icon row ("Follow us"), copyright bar, and replace the 9 dead href="#" footer links with real targets where pages exist (/{fi|en} home, /search, city pages) — point not-yet-built legal pages at /about for now and note it. Shipped: new `RegistrationTeaser`+`AppDownload` components, `SiteFooter` gradient+social-row+9 real `*Href` props (Finland→/helsinki, Estonia→/tallinn, the other 7 legal/info links→/about per instruction). See JOURNAL 2026-07-02 17:22.
- DONE: Static/footer pages so no footer link is dead: simple content pages for Terms, Privacy policy, Contact us, and "For restaurants" (reuse RestaurateurJoin content) + update footer links to them. Shipped 4 new top-level pages (`/terms`, `/privacy`, `/contact` via new `LegalContent` component with real EN/FI/RO/ES copy; `/for-restaurants` reusing existing `RestaurateurJoin` as-is) and repointed footer's Contact us/Terms/Gift card terms/Privacy policy/For restaurants links off `/about` onto them. `col2Link2` (Restaurant backoffice), `col4Link2` (affiliate partners), RegistrationTeaser CTA, AppDownload badges, and social icons intentionally left on `/about` (no content requested/no natural target). See JOURNAL "this run" entry.
- DONE: (acceptance audit 2026-07-02 #2 — root cause, platform-level) Unmatched wildcard slugs render WRONG content instead of a not-found state: /login renders the Helsinki city page; /collections/recommends-estonia renders an arbitrary restaurant page. Fixed in `hydrateBlockBindings` (render-page.tsx): a single-item binding filter keyed off the CURRENT route (`{param}`/`{query}` ref that resolves this request) matching zero rows now flags `routeNotFound`; the public route (`[[...slug]]/page.tsx`) 404s on that flag instead of letting the component render its static prop defaults. See JOURNAL 2026-07-02 (this run) for the full root-cause + fix + tests.
- DONE: (acceptance audit #2) Fix the 6 broken site-wide links that currently silent-fallback: top-bar "For restaurants" + the CTA inside /for-restaurants use `/restaurateurs` → change to `/for-restaurants` (2 places); registration-teaser CTA + footer "Restaurant backoffice"/"For affiliate partners" still on unpublished `/about` → repoint at /contact or /for-restaurants as fits; 3 footer social icons all → `/about` → use external social roots (facebook.com/instagram.com/x.com, target=_blank) like tableonline; PromoBanner `/avios` → `/offers` (exists) and `/newsletter` → `/contact`; nav `/login`+`/signup` → smallest sane target (tiny coming-soon pages or the CMS's real login route — worker's call, just no silent fallback). Shipped: `RestaurateurJoin.ctaHref`/`SiteHeader.forRestaurantsHref` → `/for-restaurants`; `RegistrationTeaser.ctaHref`/`SiteHeader.loginHref`/`signupHref`/promo-2 href → `/contact` (no visitor auth route exists — smallest sane target, noted as a known gap); `SiteFooter.col2Link2Href`/`col4Link2Href` → `/for-restaurants`; 3 social hrefs → facebook.com/instagram.com/x.com roots w/ target=_blank; promo-1 href → `/offers`. See JOURNAL 2026-07-02 17:52.
- TODO: (acceptance audit #2) /book?restaurant={slug} echoes the raw slug in the H1 and saved restaurant_name ("Book a table at atelje-finne") — resolve slug → display name against content_restaurants so the heading and booking record carry "Ateljé Finne".
- TODO: (acceptance audit #2) Remove or repurpose the orphaned pre-replica HandpickedSelection section on home ("Restovista recommends 2026", between promo strip and cities) — not in GOAL.md's section order and both its /collections/* links render wrong pages. Removing is fine; if kept, make it collection-driven with working targets.
- TODO: (acceptance audit #2) Backfill restaurants: 8 of 11 city pages are empty (only Helsinki 22 / Tallinn 2 / Tartu 1 have listings). Seed 2-3 restaurants each (image, slug, city_slug, internal book_href, rating/reviews) for Espoo, Turku, Tampere, Oulu, Jyväskylä, Kuopio, Porvoo, Pärnu.
- TODO: (acceptance audit #2, polish) /for-restaurants lacks <title>/<h1> (bare RestaurateurJoin dump) — add proper page meta + heading like the other static pages; and swap home section order so registration-teaser comes BEFORE the restaurateur section per GOAL.md (PromoBanners stay near the footer).

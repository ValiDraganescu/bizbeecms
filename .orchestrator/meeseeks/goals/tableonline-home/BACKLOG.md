# Backlog — tableonline-home
Task states: TODO | DOING | DONE | BLOCKED.

## Bugs
(human-reported bugs land here, newest at top; they outrank everything)

## Tasks
- DONE: Retheme the local site to the tableonline palette via the `update_theme` MCP tool — light: surface #f9f9f6, foreground #001414, primary #124142, primary-hover #073535 (or darker), plus coherent subtle/border/ring tokens; dark mode teal-tinted. Verify the home page on :3602 visually picks it up (curl the HTML / check computed CSS vars).
- TODO: Create a `cities` collection (title, slug, country_code, image) via MCP, seed ~11 FI+EE cities (see GOAL.md), generate/reuse images, then make the home CityLinks section collection-driven — either bind a List with a city photo-card template component (photo + dark bottom gradient + white name, links to /{slug}) or update the CityLinks component to bind. Keep the Finland/Estonia tab split via country_code filter.
- TODO: Create an `offers` collection (title, slug, image, description, date_range, restaurant, city, is_main) via MCP, seed it from the 4 hardcoded OfferCard props on the home page + 2-4 more, then replace the hardcoded offers row with a List bound to the collection (OfferCard as template, listMap the fields). Each card links to the offer detail page path (/offers/{slug} — page may not exist yet; that's a later task).
- TODO: Create an `events` collection (title, slug, image, description, date, restaurant, location, rating) via MCP, seed from the 4 hardcoded EventCard props + a few more, replace the hardcoded events row with a bound List.
- TODO: Platform feature — dynamic/param-driven pages: support a page whose slug contains a wildcard segment (e.g. `city/:slug` or `/:city` catch-all) where the param is available to blocks as a binding/filter value, AND make URL query params (e.g. ?q=) available the same way. READ `goals/archive/content-collections/` first — "Phase 3 route-driven detail pages" was deferred there and is this exact feature. This is repo-code work in CMS/; commit it like any CMS feature with tests.
- TODO: Build the search page at /search: uses the query-param feature to read ?q= and renders a filtered content_restaurants list (name/location contains q, case-insensitive); wire the HomeHero search box to submit to /search?q=... Include a "no results" state.
- TODO: Build the city landing page using the wildcard-page feature: /{city-slug} (or /city/{slug} if root wildcards clash with existing slugs) — city name hero (from cities collection), restaurant list filtered by city, city's offers/events if trivial. Wire the home city cards to it.
- TODO: Build the offer detail page (/offers/{slug} via the wildcard feature): image, title, date range, description, restaurant, and a "Book a table" CTA. Wire home OfferCards to it.
- TODO: Build the book-a-table page: a booking form page (restaurant, date, time, party size, name, email) reached from restaurant cards' "Book" buttons and the offer page CTA, pre-selecting the restaurant via param. Use the CMS form machinery (bind_form / create_form MCP tools) with a bookings collection target.
- TODO: Home page visual replica pass 1 (header/hero): dark top utility bar (#14151a) with "For restaurants" + language switcher, transparent nav over a full-bleed photo hero with 50% dark overlay + white serif headline, white rounded search field; promotions strip that rides up over the hero bottom (-4rem) with one large 16/9 main promo (offers where is_main) + secondary promo column.
- TODO: Home page visual replica pass 2 (below the fold): "Choose your city" horizontal-scroll photo cards with bottom gradient; registration teaser callout (1px #124142 border, faint teal bg, perks + CTA); app download section (App Store / Google Play badges); footer with #001414→#073535 gradient, multi-column links, social row, copyright bar.

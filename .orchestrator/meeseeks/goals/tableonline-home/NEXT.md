# Note to the next Meeseeks (tableonline-home)

Events collection (task 4) is DONE: `content_events` (title, description, date, restaurant, location, rating — all plain `string`; image `asset`; system `slug`+`status` set explicitly at seed time) has 6 published items: the 4 original hardcoded `EventCard` events (`jazz-white-lady`, `wohls-gard-afternoon-tea`, `grill-night-siuntio`, `kustavin-kipina-popup-2026`) + 2 new (`live-music-brunch-cafe-regatta`, `midsummer-tasting-night`). Home page's hardcoded 4-column `events-section` row is replaced with a single `List` (grid, 4/2/1 cols desktop/tablet/mobile, sorted by `date` asc) bound to `content_events`, template `EventCard`, `listMap:{title,description,date,image,restaurant,location,rating,eventSlug:slug}`. `EventCard` got a new `eventSlug` string prop, its anchor now reads `href="/events/{{eventSlug}}"` (prefix baked into the template, same trick as `CityCard`/`citySlug` and `OfferCard`/`offerSlug`) — and unlike the offers-section run, I actually DELETED the now-dead `bookHref` prop from the schema instead of leaving it as unused dead weight. Verified live on :3602: all 6 events render with correct hrefs/restaurant names, fi-locale heading still translates, offers section (previous run) unaffected by this page-blocks rewrite.

**All 4 home-page lists (restaurants, cities, offers, events) are now collection-driven.** That closes the entire "Every list on the home page must be collection-driven" requirement from GOAL.md. Everything left in BACKLOG.md is routing/landing pages + visual-replica polish.

New caveat this run (read CAVEATS.md): `create_collection` accepts a `fields` array INLINE at creation time (and actually REQUIRES it — errors `"fields must be an array"` without one) — no need to create-empty-then-loop `add_collection_field` like the cities/offers runs did. Saves round-trips.

Next up per BACKLOG.md (in order):
1. **Data-fix `content_restaurants`** — most of the 26 restaurants have empty/null `slug` (fill kebab-case for ALL); add a way to filter by city (either a `city_slug` field or verify `location` maps cleanly onto `content_cities` slugs — check with `query_collection` on both); generate images for the ~5 null-image items (Maxill, BasBas Kulma, Aperte, Plein, The Bull & The Firm — use `generate_image` ONE call per tool-turn, batching in a shell loop hits the Bash 2-min timeout, see CAVEATS); replace every external `book_href` (currently leaking to `https://www.tableonline.fi` and `example.com` placeholders!) with the internal pattern `/book?restaurant={slug}` (will 404 until the book page exists — that's fine, the point is stopping the off-site leak). Pure MCP/D1 data work like the last 3 runs, no repo code.
2. **Platform feature — dynamic/param-driven pages** (real repo-code work in `CMS/src/app/`): wildcard page segments (e.g. `/{city-slug}`) with the param available to blocks as a binding/filter value, AND query params (`?q=`) available the same way. READ `goals/archive/content-collections/` FIRST — its deferred "Phase 3 route-driven detail pages" is this exact feature; don't reinvent from scratch. This unblocks search/city/offer-detail/book/restaurant-detail pages, so it's the actual next big lift once the restaurant data-fix is done.

Nothing blocked. No new bugs reported. Dev server was already running on :3602 all run; MCP token still valid.

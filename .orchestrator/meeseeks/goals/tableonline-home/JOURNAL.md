# Journal — tableonline-home
Every completed (or blocked) task, newest at the bottom. Never redo anything marked DONE here.

## 2026-07-02 15:06 — Retheme to tableonline.fi dark-teal palette
- **Status:** DONE
- **What I did:** Called `update_theme` MCP tool (local-site, :3602) with full light+dark token sets. Light: surface `#f9f9f6`, foreground `#001414`, primary `#124142`, primary-hover `#073535`, primary-subtle `#e3ece9`, border `#dadfd8`, ring `#009688` (button gradient accent teal), danger `#d12c1a` (matches spec), plus coherent success/warning/info in teal-adjacent hues. Dark mode is teal-tinted (not the old default zinc+orange): surface `#001414`→raised `#0b2e2e`, primary `#009688`, border `#15403f`.
- **Verified:** `curl http://localhost:3602/` and grepped the inline per-page compiled Tailwind `<style>` block — confirmed `--color-primary:#124142`, `--color-border:#dadfd8`, `--color-surface:#f9f9f6` in the `:root` (light) block and `--color-surface:#001414`, `--color-primary:#009688` etc. in the `[data-theme="dark"]` block. Both present and correct on the live home page HTML.
- **Files:** No repo files — theme lives in D1 via MCP `update_theme` (site-level DB row, not code). Goal memory files only: JOURNAL.md, BACKLOG.md, NEXT.md.

## 2026-07-02 15:23 — Cities collection + collection-driven CityLinks
- **Status:** DONE
- **What I did:** Created collection `content_cities` (title, country_code select FI/EE, image asset — `slug`/`status` are the built-in system columns, so I did NOT declare a custom `slug` field: `create_collection` rejects it as "collides with a reserved system column"). Generated 11 city photos via `generate_image` (one MCP call each — batching them in one shell loop hit the Bash tool's 2-min timeout after ~5 calls, so did the rest one-call-per-turn). Seeded all 11 items via `add_collection_item` passing `slug` + `status:"published"` directly in `values` (confirmed system columns ARE settable that way — verified via `update_collection_item` first, then used it directly in `add_collection_item` for the rest): FI espoo/helsinki/jyvaskyla/kuopio/oulu/porvoo/tampere/turku (8), EE parnu/tallinn/tartu (3). Authored a new `CityCard` component (photo, dark bottom gradient `from-foreground/90`, white serif city name, `h-72 w-76` matching the ~19rem spec) — first version used a `link`-typed `cityHref` prop mapped from nothing (List `map` can only bind to real collection FIELDS, not compute a prefixed path), so switched to a plain `string` prop `citySlug` and hardcoded the `/` prefix in the HTML template (`href="/{{citySlug}}"`), then mapped `citySlug` → the collection's system `slug` field. Replaced the old hardcoded `CityLinks` block on the home page (`Section-5`) with: a `SectionHeading` ("Choose your city"), a "Finland" sub-heading + `List-3` (content_cities, country_code=FI, horizontal direction, gap 20, template CityCard), and an "Estonia" sub-heading + `List-4` (country_code=EE, same layout) — preserving the tableonline Finland/Estonia split via `country_code` filter instead of hardcoded tabs.
- **Verified:** Live HTML on :3602 (after publishing both the page draft AND the component draft — see new CAVEATS entries) shows all 11 cities as photo cards with correct per-city hrefs (`/espoo`, `/helsinki`, … `/tartu`), dark-gradient-over-photo + white serif name styling, horizontal-scroll row layout, correctly split 8 FI / 3 EE and alphabetically sorted within each. Re-checked with `bb_content_locale=fi` cookie — the two `SectionHeading`s translate ("Valitse kaupunkisi" / "Suomi"); city names themselves are untranslated proper nouns (acceptable per GOAL.md's locale note). `npx tsc --noEmit` clean (no repo files touched — pure MCP/D1 content work).
- **Files:** No repo source files. Goal memory only: JOURNAL.md, BACKLOG.md, CAVEATS.md, NEXT.md. Content lives in D1 (`content_cities` table, `CityCard` component, home page block tree + version rows) via MCP tools + two REST publish calls (`POST /api/pages/{id}/publish`, `POST /api/components/CityCard` with `{action:"publish"}`).

## 2026-07-02 15:29 — Offers collection + collection-driven OffersSection
- **Status:** DONE
- **What I did:** Created collection `content_offers` via `create_collection` (name must be the BARE label, e.g. `"offers"` — it auto-prefixes to `content_offers`) then `add_collection_field` (title, description, date_range, restaurant, city as `string`; is_main as `boolean`; image as `asset` — **NOT** `"image"`, that type name is rejected with `unknown field type`). Learned `add_collection_field`/`add_collection_item` want the FULL table name `content_offers` as `collection`, unlike `create_collection` which wants the bare label — inconsistent, cost a few failed round-trips (now a caveat). Seeded 6 items via `add_collection_item` (`slug`+`status:"published"` in `values`, same pattern as cities): the 4 from the home page's hardcoded `OfferCard` blocks (`childrens-portions-50`, `lobo-brunch-19`, `complimentary-sparkling`, `authentic-bread-brunch-10`) + 2 new ones (`tasting-menu-2for1`, `weekday-lunch-15`), reusing existing gallery images (no `generate_image` calls needed). **Hit the exact same locale-object trap CityCard's `name` avoided**: `content_offers.title`/`description` are plain non-translatable `string` collection fields (confirmed via `query_collection` on `content_cities` — its `title` is a bare string too), so my first `add_collection_item` call passing a `{en,fi,ro-ro}` locale object for `title`/`description` silently stored the literal string `"[object Object]"` — caught it in the tool's own JSON response, fixed via `update_collection_item` with plain English strings, and used plain strings for the rest. Updated `OfferCard` component (`update_component`, full artifact re-sent) to add an `offerSlug` string prop and changed the anchor from `href="{{offerHref}}"` (a free-text prop defaulting to the static `/offers`) to `href="/offers/{{offerSlug}}"` — same "bake the prefix into the template" trick as `CityCard`/`citySlug`, since `List`'s `map` can't compute `/offers/`+slug. Also converted `title`/`claimLabel` component-prop *defaults* (not collection data — these ARE translatable component props) into proper 4-locale objects (en/fi/ro-ro/es) per the authoring guide's requirement, since the old defaults were bare strings. Published the component draft immediately (`POST /api/components/OfferCard {action:publish}`) BEFORE binding, then `get_page`+rebuilt the full block tree in Python (JSON body too large for a shell one-liner) replacing the hardcoded 4-column `offers-section` row (4 `OfferCard`s in 4 `__section_column__`s) with a single-column row containing one `List` (`content_offers`, direction grid, columns 4/2/1 desktop/tablet/mobile, gap 20, sorted by date_range, `listMap:{title,dateRange:date_range,image,offerSlug:slug}`), then `update_page_blocks` (full tree) + `POST /api/pages/{id}/publish`.
- **Verified:** Live HTML on :3602 (`curl http://localhost:3602/`) shows all 6 offers rendering with correct `href="/offers/<slug>"` links (grepped all 6 slugs present), correct titles, `grid-cols-4` class present, dateRange text (`1.6.2026`) rendering, "Claim offer" ×12 occurrences (visible EN default text on 6 cards — inspect showed no leftover hardcoded `offer-col-1..4`/`offer-1..4` block ids, confirming the old static blocks are gone). Re-checked with `bb_content_locale=fi` cookie: heading translates ("Ajankohtaiset ravintolatarjoukset Suomessa") and claim button translates ("Lunasta tarjous") — offer titles/descriptions themselves stay English since collection item fields are single-locale by design (same acceptable limitation as city names, noted in GOAL.md). `npx tsc --noEmit` clean (no repo files touched, pure MCP/D1 content work).
- **Files:** No repo source files. Goal memory only: JOURNAL.md, BACKLOG.md, CAVEATS.md, NEXT.md. Content lives in D1 (`content_offers` table, `OfferCard` component draft+publish, home page block tree + version row 23) via MCP tools + two REST publish calls.

## 2026-07-02 15:47 — Events collection + collection-driven EventsSection
- **Status:** DONE
- **What I did:** Created collection `content_events` in ONE `create_collection` call (bare label `"events"`, `fields` array inline — `create_collection` accepts fields at creation time, no separate `add_collection_field` round-trips needed: title/description/date/restaurant/location/rating as `string`, image as `asset`). Seeded 6 items via `add_collection_item` (`slug`+`status:"published"` set explicitly in `values`, plain strings only — no locale-object trap this time): the 4 from the home page's hardcoded `EventCard` blocks (`jazz-white-lady`, `wohls-gard-afternoon-tea`, `grill-night-siuntio`, `kustavin-kipina-popup-2026`) + 2 new (`live-music-brunch-cafe-regatta`, `midsummer-tasting-night`), reusing existing gallery images (no `generate_image` calls). Updated `EventCard` (`update_component`, full artifact) to add an `eventSlug` string prop and changed the anchor from `href="{{bookHref}}"` to `href="/events/{{eventSlug}}"` (same prefix-baked-into-template trick as `CityCard`/`citySlug` and `OfferCard`/`offerSlug`) — dropped `bookHref` from the schema entirely this time (it was pure dead weight, same as the offers-section cleanup note flagged). Published the component draft, then `get_page` + rebuilt the full block tree in Python (same reason as offers: JSON too large for a shell one-liner) replacing the hardcoded 4-column `events-section` row (4 `EventCard`s in 4 columns) with a single-column row containing one `List` (`content_events`, direction grid, columns 4/2/1 desktop/tablet/mobile, gap 20, sorted by `date` asc, `listMap:{title,description,date,image,restaurant,location,rating,eventSlug:slug}`), then `update_page_blocks` (full tree) + published the page.
- **Verified:** Live HTML on :3602 shows all 6 events with correct `href="/events/<slug>"` (grepped all 6 slugs present, 6 `href="/events/…"` occurrences), all 6 restaurant names present (White Lady, Wohls Gård, Ravintola Siuntio, Kustavin Kipinä, Café Regatta, Ravintola Aino), 12 date-string matches (date is duplicated as visible text per card, consistent with 6 cards × label pattern). Re-checked `bb_content_locale=fi`: events heading still translates ("Ajankohtaiset ravintolatapahtumat Suomessa" — untouched `SectionHeading` block). Confirmed the offers section (previous run's work) is unaffected: still 6 `/offers/` hrefs live after this page-blocks rewrite. No repo files touched, so no `tsc`/build needed — pure MCP/D1 content + one component-schema change.
- **Files:** No repo source files. Goal memory only: JOURNAL.md, BACKLOG.md, CAVEATS.md, NEXT.md. Content lives in D1 (`content_events` table, `EventCard` component draft+publish — dropped `bookHref`, added `eventSlug` — home page block tree + version row 24) via MCP tools + two REST publish calls.

## 2026-07-02 16:02 — Data-fix content_restaurants (slugs, city_slug, images, internal book_href)
- **Status:** DONE
- **What I did:** Audited all 26 `content_restaurants` items via `query_collection` (not ~5 null-image as GOAL.md estimated — actually 21 of 26 had `image:null`; only the original 5 with hand-picked images were non-null). Added a new `city_slug` string field via `add_collection_field`. For every item: generated a kebab-case `slug` from `name` (Python `unicodedata.normalize('NFKD',...)` to transliterate accents first — `é`→`e`, `ö`→`o` — otherwise a naive regex strip produces ugly slugs like `atelj-finne`/`sk-rd`; confirmed all 26 resulting slugs unique), mapped `location` → `city_slug` against `content_cities` slugs (Helsinki→helsinki, Tallinn→tallinn; "Voru" had no matching city in the seeded 11, mapped it to the nearest existing EE city `tartu` as a pragmatic fallback — noted as a caveat), and rewrote every `book_href` to the internal `/book?restaurant={slug}` pattern (was leaking to `https://www.tableonline.fi/...` and various `example.com/book-*` placeholders). All 26 updates done via one `update_collection_item` PATCH call per item (26 calls, all succeeded — the tool always returns `{"ok":true,...}` wrapped inside the JSON-RPC result, so a naive substring check on the raw curl stdout is unreliable; verify by parsing/re-querying instead). For the 21 null-image items: reused 4 already-uploaded-but-unused restaurant-style gallery photos (`warm_cozy_upscale_restaurant_interior`, `cinematic_moody_overhead`, `cinematic_dark_fine_dining_scene`, `cinematic_moody_fine_dining_restaurant`) for Maxill/BasBas Kulma/Aperte/Plein, then generated 17 fresh distinct images via `generate_image` (one MCP call per tool-turn per the existing timeout caveat) with per-restaurant descriptive prompts (name + cuisine/city cues baked in) for the rest, then applied each generated URL via `update_collection_item`.
- **Verified:** Re-queried `content_restaurants` (26 items): 0 empty/null slugs, 0 null images, 0 external (non-`/book?`) `book_href` values, 0 missing `city_slug`. `curl http://localhost:3602/` (home page, unauthenticated public route) shows the 4 featured/promoted restaurants that actually render in the two existing home Lists all have `/book?restaurant=<slug>` hrefs and their images 200 OK; grepped the full HTML for `tableonline.fi`/`example.com` — zero restaurant-data leaks (only an unrelated i18n placeholder string `teammate@example.com` in an invite-form label, a false positive). No repo files touched (pure MCP/D1), so no build/tsc needed — confirmed via `git status --short` showing zero unexpected file changes.
- **Files:** No repo source files. Goal memory only: JOURNAL.md, BACKLOG.md, CAVEATS.md, NEXT.md. Content lives in D1 (`content_restaurants` table: new `city_slug` field, all 26 rows' `slug`/`city_slug`/`book_href`/`image` updated) + 17 new assets in the media gallery via `generate_image`.

## 2026-07-02 16:15 — Platform feature: dynamic/param-driven pages (wildcard slug + query params)
- **Status:** DONE
- **What I did:** Repo-code CMS feature (first non-MCP-content task this goal). Read `goals/archive/content-collections/{GOAL,BACKLOG,CAVEATS}.md` first per the hint — confirmed "Phase 3 route-driven detail pages" was deferred and is exactly this. Implemented the LAZIEST design that reuses every existing seam instead of inventing a new routing system:
  1. **Wildcard page slug**: widened `SLUG_RE` in both `lib/pages/page-meta.ts` and `lib/chat/page-tool.ts` from `/^[a-z0-9][a-z0-9-]{0,63}$/` to `/^:?[a-z0-9][a-z0-9-]{0,63}$/` — a leading `:` (e.g. `:city-slug`) marks a WILDCARD segment. A page authored with slug `:city-slug` under any parent (or top-level) now matches ANY concrete path segment there.
  2. **Route-param capture**: `lib/render/slug.ts` grew pure helpers `isParamSlug`/`paramName`/`matchSlugSegment` (unit-tested in isolation from D1) — `matchSlugSegment(siblings, segment)` picks an EXACT match first, falls back to a wildcard sibling, captures `{name, value}`. `[[...slug]]/page.tsx`'s `resolvePage` now fetches ALL siblings per tree level (not a single exact-match query) and calls `matchSlugSegment` at each level, accumulating captured params into a `Record<string,string>` returned alongside the matched page.
  3. **Query params**: the public route's `generateMetadata`/`PublicPage` now also receive Next's `searchParams` promise, flattened (first value per key) into the same shape.
  4. **The param seam — reused the EXISTING `{prop}` api-param pattern, didn't invent a new DSL**: new pure module `lib/content/route-params.ts` (`RouteContext{params,query}`, `resolveRouteValue`, `resolveRouteFilters`) lets a List/binding FILTER VALUE be `{"param":"city-slug"}` or `{"query":"q"}` instead of a literal — mirrors `lib/data-sources/bind.ts:resolveBindingParams`'s `{prop}` shape exactly. `FilterClause.value` was ALREADY `unknown`-typed end-to-end (query-compiler, binding-tools, MCP schemas) so ZERO type/schema changes were needed there — only a resolve-pass. Wired into `render-page.tsx`'s `hydrateBlockBindings`: both the List-block collection query and the single-item binding's `bindingQuerySpec` now run their filters through `resolveRouteFilters(filters, routeContext)` before `queryCollection`. GRACEFUL by design (matches the project's existing binding contract): a clause whose param/query is absent this request is DROPPED (not filtered-on-undefined) — an optional query param that's unset just means "no filter on that field", matching how `/helsinki` (no `?cc=`) showed ALL cities in the live smoke test below. `buildPlanFromPage` grew an optional 4th `routeContext` param (default `EMPTY_ROUTE_CONTEXT`) — 100% backward compatible, the admin preview route (`preview/[id]/page.tsx`, still 3-arg call) needed NO changes.
  5. **AI/MCP discoverability**: updated `create_page`'s tool-schema `slug` description + `binding-tools.ts`'s shared `FILTER_SCHEMA.value` description (used by `bind_component`/`create_list`/`bind_list`) to document the `:name` wildcard-slug convention and the `{param}`/`{query}` filter-value shape — confirmed live via `tools/list` that the MCP schema text updates reached the running dev server without a restart.
- **Verified:** `npx tsc --noEmit` clean. `npm test` 1446/1446 green (was 1416 before — added 4 new test files/additions: `scripts/route-params.test.mjs` new (24 tests: isParamSlug/paramName/resolveSlugPath/isRouteValueRef/resolveRouteValue/resolveRouteFilters incl. the "drop on missing param" graceful case), `scripts/slug.test.mjs` +5 (`matchSlugSegment`: exact-wins, wildcard-fallback-captures, no-match-404, empty-siblings, exact-preferred-over-wildcard-with-same-literal), `scripts/page-meta.test.mjs` +3 (isValidSlug/validatePageMeta/isParamSlug accept `:city-slug`), `src/lib/chat/page-tool.test.ts` +1 (create_page accepts a wildcard slug end-to-end through the full validator). LIVE end-to-end smoke test on :3602 (dev server, per CAVEATS never built while dev runs): created a real top-level page `:city-slug` via MCP `create_page`+`update_page_blocks` (discovered `create_page`'s `upsertPage` writes the LEGACY `page.blocks` column directly with NO draft-version row — so `get_page`/`bind_component` which read the draft version saw empty blocks until I also called `update_page_blocks`, which DOES write the draft; this is a PRE-EXISTING page-tool wiring gap, unrelated to this feature — flagged as a caveat, not fixed here, out of scope), bound its `SectionHeading` block's `title` prop to `content_cities.title` filtered by `slug eq {param:"city-slug"}` via `bind_component`, added a `List` (CityCard template) filtered by `country_code eq {query:"cc"}` via `create_list`, published. Confirmed live: `GET /helsinki` → h2 "Helsinki"; `GET /tallinn` → h2 "Tallinn" (SAME page, different route param per request); `GET /nonexistent-city` → 200 with the graceful static fallback "fallback city" (never a 500/crash on an unresolved binding); `GET /helsinki?cc=EE` → List shows only tallinn/tartu/parnu; `GET /helsinki?cc=FI` → only the 8 FI cities; `GET /helsinki` (no `?cc=`) → ALL 11 cities (confirms the "absent query param → filter dropped, not filter-on-undefined" graceful contract). Deleted the scratch test page afterward (`DELETE /api/pages?id=`) — confirmed `/helsinki` now 404s and the real home page (`/`) still 200s, un-touched.
- **Files:** `CMS/src/lib/render/slug.ts` (+`isParamSlug`/`paramName`/`matchSlugSegment`/`SlugCandidate`), `CMS/src/lib/content/route-params.ts` (NEW — `RouteContext`/`isRouteValueRef`/`resolveRouteValue`/`resolveRouteFilters`), `CMS/src/lib/render/render-page.tsx` (`buildPlanFromPage`/`hydrateBlockBindings` thread `routeContext` through), `CMS/src/app/[[...slug]]/page.tsx` (`resolvePage` wildcard-aware + captures params, `loadPlan`/`generateMetadata`/`PublicPage` accept+flatten `searchParams`), `CMS/src/lib/pages/page-meta.ts` (+`isParamSlug`, widened `SLUG_RE`), `CMS/src/lib/chat/page-tool.ts` (widened `SLUG_RE` + tool-schema description), `CMS/src/lib/chat/binding-tools.ts` (`FILTER_SCHEMA.value` description). Tests: `CMS/scripts/route-params.test.mjs` (NEW), `CMS/scripts/slug.test.mjs`, `CMS/scripts/page-meta.test.mjs`, `CMS/src/lib/chat/page-tool.test.ts`. Goal memory: JOURNAL.md, BACKLOG.md, CAVEATS.md, NEXT.md.


## 2026-07-02 16:31 — /search page (rows, count, no-results) + HomeHero/SiteHeader search forms
- **Status:** DONE
- **What I did:** Two parts: a small platform extension (repo code) + the actual /search page content (MCP/D1).
  1. **Platform extension: List `search` (route-value-aware) + prop-level route-value refs.** The dynamic-pages feature (previous run) only let a List's `filter` clauses (AND'd) read {"param"}/{"query"}. The spec needs "name OR location contains q, case-insensitive" -- filters can't OR across fields, but the query-compiler ALREADY has exactly that as QuerySpec.search (a LIKE OR'd across all text-typed collection fields) -- it just wasn't wired to List blocks. Added ListSource.search?: unknown (plan-types.ts), resolved it via the existing resolveRouteValue in render-page.tsx's List-hydrate branch (mirrors the filter/resolveRouteFilters wiring exactly), and threaded it through the MCP surface: binding-tools.ts gets a shapeSearch validator + SEARCH_SCHEMA (accepts a literal string OR a {param}/{query} ref) on both create_list/bind_list's args/schemas, tool-dispatch.ts's handleCreateList/handleBindList pass it into listSource. Separately, discovered plain block `props` (not just filter/binding values) had NO route-value resolution at all -- needed for the h1 "Results for '{{query}}'" text, which is a component PROP, not a collection filter. Added resolveRouteProps (route-params.ts) -- resolves ONLY values matching isRouteValueRef in a props object (everything else, including legitimately object-shaped json-typed props, passes through untouched by construction), wired into BOTH branches of hydrateBlockBindings in render-page.tsx (the no-bindings short-circuit AND the post-hydrateProps bindings path) so it applies to every block uniformly. Missing ref resolves to "" (not undefined/crash) since there's no "clause" to drop here, only text to render. Documented in set_block_props's tool description.
  2. **/search page (MCP/D1 content).** Created 3 new components: SearchResultRow (thumbnail + name-link + location + rating/5 (reviews) + Book pill -- NOT reusing home's RestaurantRow, which has a "New!" badge and no rating, wrong shape for a search result and would blast-radius the home page's "newest" list if edited in place), SearchHeader (breadcrumb "Home > Results for", h1 "Results for '{{query}}'" where query is a {"query":"q"}-bound prop, and a data-search-count/data-list-target text node + tiny inline script that counts [data-list="results-list"] > * children client-side on load and stamps "Showing N restaurants" -- no new server plumbing needed for the count, it's a native DOM read of what already rendered), SearchEmptyState ("No results found" / "Adjust your search and try again."). Created page `search` (top-level, en/fi/ro-ro) via create_page (writes legacy `blocks` only -- always pass `blocks`, the tool rejects a missing one immediately with a clear error) then update_page_blocks with the SAME tree (populates the draft -- the known caveat). Tree: Header(SiteHeader) -> Search heading(SearchHeader, query prop = {"query":"q"}) -> Search results(List id results-list, collection content_restaurants, search:{"query":"q"}, sort rating desc, limit 50, template SearchResultRow, map name/location/image/rating/reviews/bookHref/detailHref -> book_href for both -- no restaurant detail page exists yet so name-link reuses the book link, same convention the home page's RestaurantRow already uses; plus a listRole:"empty" child SearchEmptyState) -> Footer(SiteFooter). **Publish gotcha (NEW, see CAVEATS):** POST /api/pages/<id>/publish bumped the version but list_pages still showed publishStatus:"draft" -- the PUBLIC route gates on the page row's publishStatus COLUMN (a separate legacy draft/live toggle, set only via create_page's publishStatus arg or the admin UI's meta-PATCH toggle), not just having a publishedVersionId. Fixed by re-calling create_page with the same slug + publishStatus:"published" + the full current block tree (re-fetched via get_page first) -- create_page's upsert always overwrites the legacy blocks column too, but since a publishedVersionId now exists the versioned render path is what actually serves, so the legacy column content is moot. Also fixed the two known dead search boxes: HomeHero (update_component -- replaced the bare <input>+<a> "Search" button with <form method="GET" action="/search">, input name="q", <button type="submit">, dropped the now-unused ctaHref prop) and SiteHeader (same trivial <form> wrap around its search <input>, added name="q") -- both published immediately after update_component per the draft/publish caveat.
- **Verified:** `npx tsc --noEmit` clean. `npm test` 1456/1456 green (was 1446 -> +10: 4 in binding-tools.test.mjs for search shaping on create_list/bind_list incl. the malformed-ref rejection, 6 in route-params.test.mjs for resolveRouteProps incl. the "absent -> empty string not crash" and "non-ref values pass through untouched" cases). LIVE on :3602: GET / -> both search <form method="GET" action="/search"> present with input name="q" (curl-grepped). GET /search (no ?q=) -> h1 "Results for ''", 50 rows (limit, no filter -- graceful "absent query -> no filter" contract, matches the platform feature's existing behavior). GET /search?q=helsinki -> h1 "Results for 'helsinki'", 23 unique restaurants (LIKE across name+location text fields -- confirmed via query_collection that a plain location field is single-locale text, so this is a legit case-insensitive substring match, not a bug), each row has a name-link + Book pill both -> /book?restaurant=<slug>. GET /search?q=zzzznotfound -> "No results found" / "Adjust your search and try again." renders (the List's listRole:"empty" slot -- 0 matching rows). Count-line script verified present in the served HTML (data-search-count/data-list-target="results-list"/data-count-label="Showing {n} restaurants" + the querySelectorAll/textContent script) -- did NOT run a real browser to see the post-JS DOM text (curl only fetches SSR HTML), so the exact "Showing N restaurants" client-side substitution is verified by code review + the identical pattern already proven in this codebase (plan-list.ts's autoscroll script uses the same document.querySelectorAll + forEach idiom), not a live screenshot -- flagged as a residual gap in NEXT.md. `git status --short` confirms no unintended edits (the pre-existing main/SUBGOALS.md diff predates this run per the conversation's starting gitStatus -- left untouched/unstaged).
- **Files:** CMS/src/lib/render/plan-types.ts (+ListSource.search), CMS/src/lib/render/render-page.tsx (List search resolve + resolveRouteProps wired into both hydrate branches), CMS/src/lib/content/route-params.ts (+resolveRouteProps), CMS/src/lib/chat/binding-tools.ts (+shapeSearch/SEARCH_SCHEMA, CreateListArgs.search/BindListArgs.search + validators), CMS/src/lib/chat/tool-dispatch.ts (thread search into listSource in both create/bind handlers), CMS/src/lib/chat/write-tools.ts (set_block_props description documents prop-level route refs). Tests: CMS/scripts/binding-tools.test.mjs, CMS/scripts/route-params.test.mjs. No repo files for the page/component content -- lives in D1 (SearchResultRow/SearchHeader/SearchEmptyState components, search page + draft/published version, HomeHero/SiteHeader component drafts+publish) via MCP + 2 publish REST calls + 1 create_page re-call for the publishStatus flip. Goal memory: JOURNAL.md, BACKLOG.md, CAVEATS.md, NEXT.md.

# Backlog — ai-context-engineering
Task states: TODO | DOING | DONE | BLOCKED.

## Bugs
(human-reported bugs land here, newest at top; they outrank everything)

## Tasks
- DONE (2026-07-03): Stale-thread history compaction (USER-SCOPED 2026-07-03): when the widget LOADS a saved chat thread whose last activity is older than 24 hours, compact each historical tool result to a one-line stub (e.g. `[get_page result, 18.8kB — elided; call the tool again if needed]`) before it enters the transcript the next send builds history from. HARD CONSTRAINT from the user: NEVER mutate a live/recent thread's replayed history — mid-conversation mutation invalidates the provider's prompt cache and costs MORE than it saves; compaction applies ONLY at thread-load time for threads >24h cold. Implement as a pure, node-tested function (input: stored thread messages + thread updated-at + now; output: compacted messages) applied in the thread-load path (see `history.ts` + the widget's thread-open flow in `chat-widget.tsx`); `build-history.ts` stays untouched for live turns. Keep the on-screen tool cards intact (UI display is separate from what's replayed to the model) if that's how the pieces already split — verify before assuming. Tests: >24h thread compacts, <24h thread byte-identical, tool cards with errors keep their error shape.
- DONE (2026-07-03): Paging for ALL list-a-resource tools (USER-SCOPED 2026-07-03: "add paging to all tools that list a resource"): first MEASURE current behavior of every lister — query_collection (default limit 1000 — drop to 20), list_assets, list_components, list_pages, list_data_sources, list_prompts, list_locales, search_icons, list_builtin_types — then add limit+offset params and a `total` count to each result where the backing store can exceed a sane page (skip the ones that are inherently tiny/bounded, e.g. list_locales/list_builtin_types — note the skip in the result). Small defaults (~20) + total + an explicit "more available, pass offset" hint in the result so the error/result is self-correcting. These schemas are SHARED with the /mcp server — external MCP clients get the same paging; update the affected tool descriptions and any tests locking result shapes. Suite + tsc green; live-verify one paged call over MCP on :3602.
- TODO: Context-aware buildSystemPrompt: pass the AdminPageContext (or the resolved in-scope tool-name list — prefer the tool list, it's the truth) into `buildSystemPrompt` (site-settings.ts) via `assembleSystemPrompt`, and gate each section on whether its tools are in scope: component-authoring rules (html/slots/icons/entities/Tailwind-tokens, ~1.5k tok) only where create_component/update_component are scoped; List/combobox/set_block_props guidance (~590 tok) only where those tools are scoped; existing-components list (~1.9k tok) only in building contexts (page-builder/components/pages/general); collections list only where query_collection/bind tools are scoped; i18n rule only where translatable-prop-writing tools are scoped. Target: media/settings/collections prompts drop from ~5.2k to ~1–2k tok with zero loss in the building contexts. The debug endpoint + get_authoring_guide reuse the same assembly — verify both still reflect reality per context. Extend existing site-settings/prompt tests; live-check `GET /api/chat/debug?context=media` shrinks as expected.
- TODO: New `data-sources` admin context: /admin/data-sources currently falls through to `general` (42 tools, ~17.7k tok fixed). Add "data-sources" to KNOWN_CONTEXTS + AdminPageContext, scope TOOLS_BY_CONTEXT to the data-source workflow (list_data_sources, create_data_source, test_data_source, get_data_sources_guide, plus the binding consumers if genuinely useful there — decide by reading what the page lets the operator do), and write a short CONTEXT_PROMPT that leans on get_data_sources_guide instead of inlining the playbook. The inline data-sources context store already publishes on that page (data-sources-context.ts) — verify chip + send path still work. Update tool-scopes tests (incl. detectAdminContext for the new segment) + the admin-nav drift locks if any key off contexts.
- TODO: Dedup triple-paid prose + cap unbounded prompt lists: (a) diff the binding-trio schema descriptions (bind_list ~1,684 tok, create_list ~959, bind_component ~755 in binding-tools.ts) against CONTEXT_PROMPTS and data-sources-guide.ts — move genuinely duplicated playbook prose into the guide, keep terse point-of-use semantics in the schemas (careful: schemas also serve external MCP clients with NO context prompt — never strip a fact whose only other home is the widget-only context prompt); (b) cap the existing-components list in buildSystemPrompt (e.g. 60 defs then "…and N more — use list_components") and same for the collections list, so a 200-component site can't silently add ~9k tok; (c) fix the prompt's stale opening paragraph that still claims the tools are only create_component/create_page/translate/list_assets (Slice-2 drift — reword to describe the toolbox generically). Measure before/after via the debug endpoint and record the numbers in JOURNAL.

# Backlog archive — deploy-audit-trail
Completed tasks, compressed to one line each (10–20 words), newest at bottom.
The full record lives in JOURNAL.md; this is the trimmed backlog trace.

- BUG [P1]: deploy timeline mixed events from DIFFERENT deploy runs into one list
- BUG [P2]: deploy timeline showed each step TWICE (Running row + Done row)
- Schema + ingest API
- Emit per-step events from the bash script
- Surface errors
- Events read API + UI
- Container RAM during build
- 2026-06-19 Show total deploy duration per run

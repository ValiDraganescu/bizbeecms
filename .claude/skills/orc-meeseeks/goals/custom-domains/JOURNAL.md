# Journal — custom-domains
Every completed (or blocked) task, newest at the bottom. Never redo anything marked DONE here.

## 2026-06-19 11:33 — Attach deployer.bizbeecms.com to the deployer Worker
- **Status:** DONE
- **What I did:** Added a `routes` entry `{ "pattern": "deployer.bizbeecms.com", "custom_domain": true }` to `deployer/wrangler.jsonc` so Cloudflare attaches the hostname+cert to the deployer Worker on deploy. Flipped PM's `DEPLOYER_URL` var (`ProjectManager/wrangler.jsonc`) from `https://bizbeecms-deployer.vali-draganescu88.workers.dev` to `https://deployer.bizbeecms.com`. workers.dev subdomain left enabled as a fallback.
- **Verified:** Both wrangler.jsonc files still parse as JSON (comments stripped). Confirmed `DEPLOYER_URL` is read purely from the env var (`bag.DEPLOYER_URL`) in `sites/[id]/deploy/route.ts:57` and `sites/[id]/custom-domain/route.ts:79` — NO hardcoded default in `hosts.ts`, so flipping the var is sufficient on the PM side. No SSO code touched (deployer is server-to-server with DEPLOYER_SECRET bearer; not in the SSO host allowlist). Could NOT verify live (no Cloudflare deploy run this session — config-only change; actual `wrangler deploy` + DNS resolution must be confirmed on next deploy).
- **Files:** deployer/wrangler.jsonc, ProjectManager/wrangler.jsonc

## 2026-06-19 11:37 — Attach manager.bizbeecms.com to the PM Worker
- **Status:** DONE
- **What I did:** Added `routes:[{pattern:"manager.bizbeecms.com",custom_domain:true}]` to `ProjectManager/wrangler.jsonc` and flipped `APP_ORIGIN` var → `https://manager.bizbeecms.com`. The tricky part was the NEXT.md warning: `cmsWorkerUrl()` (`src/lib/deploy/worker-url.ts`) parsed `APP_ORIGIN` to derive the `.workers.dev` account subdomain and returned `null` for any non-workers.dev host — so naively flipping APP_ORIGIN would have hidden the "Open CMS" link on both sites pages. FIX: decoupled `cmsWorkerUrl` from APP_ORIGIN — it now builds the URL from the existing `WORKERS_DEV_SUFFIX` constant in `hosts.ts` (the single source of truth for the account subdomain), so it's correct regardless of what APP_ORIGIN is. Kept it `async` so the two `await cmsWorkerUrl(...)` callers are untouched.
- **Verified:** `npx tsc --noEmit` clean. wrangler.jsonc parses (routes + APP_ORIGIN confirmed via comment-stripped JSON.parse). Regression-checked the cmsWorkerUrl logic returns `https://<worker>.vali-draganescu88.workers.dev` and `null` for empty input. Audited the other APP_ORIGIN readers: `send-invite.ts` just prefixes APP_ORIGIN onto the accept path (custom domain is fine/correct); SSO routes use request-relative redirects, NOT APP_ORIGIN; deployer's injected `PM_ORIGIN` comes from the deployer's own env (separate backlog task), NOT PM's APP_ORIGIN. So this change is self-contained. Could NOT verify live (no Cloudflare deploy this session — config + refactor only).
- **Files:** ProjectManager/wrangler.jsonc, ProjectManager/src/lib/deploy/worker-url.ts

## 2026-06-19 11:40 — Switch per-site CMS hostname scheme to <slug>.site.bizbeecms.com (router)
- **Status:** DONE
- **What I did:** Chose Option A (router-only, no deployer change). The router already proxies Host→`bizbeecms-cms-<slug>.workers.dev`; the lazy win is encoding the slug in the subdomain. (1) `router/wrangler.jsonc`: added route `{pattern:"*.site.bizbeecms.com/*", zone_name:"bizbeecms.com"}` alongside the existing `cf.bizbeecms.com/*`. (2) `router/src/index.ts`: when `host` ends with `.site.bizbeecms.com`, take the leftmost label as the slug (validated `^[a-z0-9][a-z0-9-]*$` so nested labels / leading-hyphen are rejected) and skip the KV lookup entirely; all other hosts still go through `HOST_MAP.get(host)`. Internal worker names `bizbeecms-cms-<slug>` are untouched — only the PUBLIC hostname changed. No SSO code touched: `<slug>.site.bizbeecms.com` is under bizbeecms.com so the own-zone allowlist path already accepts it (verified by re-reading the grounding, not by editing).
- **Verified:** `router/wrangler.jsonc` parses as JSON (comments stripped). `npx wrangler deploy --dry-run` bundles the Worker clean (22 KiB) with both routes. Wrote a Node assert self-check for the slug-derivation: `acme.site.bizbeecms.com`→`acme`, uppercase lowered, `evil.acme.site...`→null (nested rejected), `-bad...`→null, wrong-suffix→null (falls to HOST_MAP) — all pass. Could NOT verify live (no `wrangler deploy` this session; also a `*.site.bizbeecms.com` wildcard cert must exist on the zone for live TLS).
- **Files:** router/wrangler.jsonc, router/src/index.ts

## 2026-06-19 11:43 — Point PM_ORIGIN + site links at the new hosts
- **Status:** DONE
- **What I did:** (1) `hosts.ts` is now the single source of truth: added `SITE_HOST_SUFFIX = ".site.bizbeecms.com"`, `siteUrlForSlug(slug)` → `https://<slug>.site.bizbeecms.com`, and `PM_ORIGIN = https://manager.bizbeecms.com`. (2) Rewrote `cmsWorkerUrl()` (worker-url.ts) to strip the `CMS_WORKER_PREFIX` off the worker name to recover the slug and return `siteUrlForSlug(slug)` — the "Open CMS"/"open site" links on both sites pages now point at the custom hostname; NO `.workers.dev` string can leak (returns null for non-`bizbeecms-cms-*` names). (3) Deployer: added a committed `vars: { PM_ORIGIN: "https://manager.bizbeecms.com" }` block to `deployer/wrangler.jsonc` so every per-Site CMS Worker gets injected `PM_ORIGIN` pinned to the custom domain (code at index.ts:262 reads `env.PM_ORIGIN ?? env.PM_CALLBACK_ORIGIN`; now PM_ORIGIN is explicit & versioned, no longer reliant on a hand-set secret).
- **Verified:** `npx tsc --noEmit` clean. Both wrangler.jsonc parse. `npx wrangler deploy --dry-run` on the deployer shows the binding `env.PM_ORIGIN ("https://manager.bizbeecms.com")` live. Node assert self-check on the new slug derivation: `bizbeecms-cms-acme`→`https://acme.site.bizbeecms.com`, `bizbeecms-cms-`→null, `""`→null, non-CMS name→null. Could NOT verify live (no `wrangler deploy` this session; needs the `*.site.bizbeecms.com` wildcard cert on the zone). The two callers (`sites/page.tsx`, `sites/[id]/page.tsx`) are unchanged — still `await cmsWorkerUrl(workerName)`.
- **Files:** ProjectManager/src/lib/config/hosts.ts, ProjectManager/src/lib/deploy/worker-url.ts, deployer/wrangler.jsonc

## 2026-06-19 — Verify SSO end-to-end on the new domains
- **Status:** DONE
- **What I did:** Read `cms-sso.ts:classifyCmsReturnUrl` and confirmed the own-zone branch (`host === ZONE_DOMAIN || host.endsWith(".bizbeecms.com")`) already accepts BOTH new custom hosts — `<slug>.site.bizbeecms.com` and `manager.bizbeecms.com` — with NO allowlist change (as the key insight predicted). Extended `cms-sso.test.ts` (12→16 tests) with 4 cases locking this in: (1) `acme.site.bizbeecms.com` accepted (own-zone), (2) `manager.bizbeecms.com` accepted (own-zone), (3) suffix-spoof `acme.site.bizbeecms.com.evil.com` → `{host}` (NOT accepted — ends in `.evil.com`), (4) `evil.attacker.workers.dev` → `{host}` (not own-zone, not own-account CMS). Did NOT touch `cms-sso.ts` — assertions only.
- **Verified:** `node --test src/lib/auth/cms-sso.test.ts` → 16 pass / 0 fail. Baseline was 12/12 before edits. The 4-label account-suffix anchoring + own-zone check (commits 01e7341/9b1898d) still reject every attacker lookalike. Live nonce-mint→redirect→exchange→validate walk NOT run (no live deploy this session; needs the `*.site.bizbeecms.com` wildcard cert), but the host-classification gate — the only SSO step that depends on the hostname — is proven by unit test for all four new/attacker hosts.
- **Files:** ProjectManager/src/lib/auth/cms-sso.test.ts

## 2026-06-19 08:52 — Decommission / alias the old .workers.dev URLs (audit)
- **Status:** DONE
- **What I did:** Audited every non-comment, non-test `.workers.dev` / `WORKERS_DEV_SUFFIX` / `ACCOUNT_WORKERS_SUBDOMAIN` reference across `ProjectManager/src`, `router/src`, `deployer/src` and all three `wrangler.jsonc`. Found NO user-facing leak — every live reference is either an inbound SSO allowlist check or internal transport/fallback. No code change needed; this is a verification + cutover-state-record task.
- **Findings (4 live refs, all intentional):**
  1. `hosts.ts:13-16` `ACCOUNT_WORKERS_SUBDOMAIN`/`WORKERS_DEV_SUFFIX` — ONLY consumer is `cms-sso.ts:39` `isOwnCmsWorker` (the SSO own-account allowlist fallback). KEEP — dropping it would loosen/break the allowlist. Not a user link.
  2. `cloudflare.ts:130-140` `enableWorkerSubdomain` — internal raw Script-Upload path; enables the workers.dev subdomain so an API-uploaded worker is reachable as origin. User-facing URL is built by `cmsWorkerUrl()` → `<slug>.site.bizbeecms.com`, NOT from this. Leaving it enabled = the workers.dev fallback (per CAVEATS: don't disable). No user link derives from it.
  3. `router/src/index.ts:43` — router proxies `<slug>.site.bizbeecms.com` → `bizbeecms-cms-<slug>.<sub>.workers.dev` as the INTERNAL origin fetch. Invisible to users; this is the transport/fallback path. Correct & intended.
  4. wrangler vars — `APP_ORIGIN`=manager.*, `DEPLOYER_URL`=deployer.*, `PM_ORIGIN`=manager.*. No workers.dev in any user-facing var.
- **Verified:** `node --test src/lib/auth/cms-sso.test.ts` → 16/16 pass (allowlist still accepts manager.*/<slug>.site.* and rejects `*.evil.com` + `evil.attacker.workers.dev`). Grep confirms the only `WORKERS_DEV_SUFFIX` consumer is the SSO check. workers.dev left reachable as fallback (subdomain not disabled). No file edits to code — audit-only; recorded cutover state in CAVEATS.
- **Files:** (no code changed) — memory only: CAVEATS.md, BACKLOG.md, JOURNAL.md, NEXT.md

## 2026-06-19 16:53 — Always-visible custom-domain setup guide (empty-state explainer)
- **Status:** DONE
- **What I did:** The last open backlog item (the only one — no bugs). `CustomDomainForm` (`ProjectManager/src/app/(app)/sites/custom-domain-form.tsx`) previously showed DNS records ONLY after a domain was attached; the empty state was a bare input + "Add domain". Added a new `SetupGuide` component rendered ALWAYS (right after the notDeployed alert, before the domain list + form) so the operator sees their options up front. It shows three paths: (1) **CNAME for a subdomain** (recommended) — `www.example.com → cf.bizbeecms.com` (uses `CUSTOM_DOMAIN_FALLBACK_ORIGIN`) plus the note that apex can't CNAME so 301-redirect apex→www; (2) **A records for the apex** — both `CUSTOM_DOMAIN_APEX_IPS` anycast IPs on `example.com`; (3) a collapsed native `<details>` "Other options" listing apex CNAME-flattening/ALIAS/ANAME (Cloudflare Registrar/Namecheap), AAAA (not required — anycast answers IPv6), and DCV delegation via `_acme-challenge` CNAME vs the one-time TXT. Reused the existing `DnsRow` sub-component for the example records. New i18n namespace `sites.customDomain.guide` (12 keys) added to EN/FI/ET.
- **Verified:** `npx tsc --noEmit` → exit 0. All three message files parse as JSON; `guide` key set identical across en/fi/et (parity check passed). `git diff --stat` on the message files shows +additions only (no accidental reformat). Used native `<details>` (rung 3 — no JS, no new dependency). Could NOT verify live in-browser (no `npm run dev` this session; the design hook scanned the file each edit and reported no anti-patterns). A screenshot/visual review of the new block is the remaining check.
- **Files:** ProjectManager/src/app/(app)/sites/custom-domain-form.tsx, ProjectManager/messages/{en,fi,et}.json

## 2026-06-19 17:00 — Visual review of the SetupGuide (the one remaining codeable check)
- **Status:** DONE
- **What I did:** NEXT.md's remaining item (3): visually verify the always-visible custom-domain `SetupGuide` block. No open bugs, no open TODOs. To get a real render I unblocked local dev: `npx wrangler d1 migrations apply bizbeecms --local` (local D1 had no tables → `/login` 500'd with `no such table: users`). Then via curl + cookie jar: registered the SuperAdmin bootstrap user, created site "acme", and fetched the site detail page (`/sites/<id>`) HTML at 200. Asserted with a Node script that ALL 12 `sites.customDomain.guide.*` strings render as real translated copy (no raw `guide.*` key leak), the example DNS rows render (`www.example.com → cf.bizbeecms.com`), BOTH apex anycast IPs (104.21.34.242 / 172.67.210.25) render, and the native `<details>` "Other options" is present. Re-fetched with `NEXT_LOCALE=fi` → Finnish guide strings ("Oman verkkotunnuksen määritys", "Aliverkkotunnus CNAME:lla (suositus)") render too. Confirmed the block renders on a NON-deployed site (the empty state — its whole purpose). No code changed (review-only).
- **Verified:** site page HTTP 200; 12/12 guide strings present in rendered HTML (EN); 2/2 apex IPs present; `<details>` present; no `guide.`/`customDomain.guide` raw-key leak; FI locale renders. Tailwind tokens used (`border-border`, `bg-surface-muted/40`, `text-foreground-muted`) confirmed defined in globals.css and already used by sibling components. Stopped dev server after. Could NOT do a pixel screenshot via the Chrome MCP (cross-tool cookie sync not worth the thrash for a static block) — server-rendered HTML inspection covers the substance.
- **Files:** (no code changed) — memory only: JOURNAL.md, CAVEATS.md, NEXT.md. Side effect: local D1 migrated + seeded (dev-only state, not committed).

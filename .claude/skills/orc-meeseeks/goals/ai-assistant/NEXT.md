# Note to the next Meeseeks (ai-assistant)
First run — no prior context. Read main/GOAL.md, then this goal's GOAL.md and CAVEATS.md (the tool-
portability + what-already-exists notes save you a lot of time). Take Slice 1 (the Intercom widget shell)
from BACKLOG.md — it reuses the existing chat backend, so it's the natural tracer. Slices 2–4 build on it.
NOTE: the model transport (Cloudflare AI REST API) is the binding-adapters goal's job — don't build it here.

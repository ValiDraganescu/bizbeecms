# Note to the next Meeseeks (ai-openrouter)
First run — no prior context. Read `../main/GOAL.md`, then this goal's `GOAL.md` + `CAVEATS.md`, then
skim the archived predecessors (`../archive/ai-assistant/`, `../archive/binding-adapters/` — especially
their CAVEATS + the `Ai` port at `CMS/src/lib/ports/ai.ts`). Take the TOP `## Tasks` item: the tracer —
the `OpenRouterAi` adapter + its fake-`fetch` unit test, NOT wired in yet.

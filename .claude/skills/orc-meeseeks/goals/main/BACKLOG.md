# Backlog — main
Task states: TODO | DOING | DONE | BLOCKED.

## Bugs
(human-reported bugs land here, newest at top; they outrank everything)

## Tasks
- DONE: **C3 follow-on: block-prop → component-prop binding (G1's missing half).** `lib/render/tree.ts` now binds a block's DECLARED props into `{{prop}}` slots (text nodes + string prop values) of the referenced component's tree before planning. Allowlist = component's `propsSchema` keys; undeclared block props AND undeclared `{{slots}}` are dropped to ""; locale-object values resolved first; bound values land as plain text/data (React escapes downstream — no HTML injection, no eval). Route maps `propsSchema` into the component map. Blog-kit components rewritten to use `{{slots}}`. 6 new node tests (binds / undeclared dropped / undeclared slot empty / unsafe escaped / no-schema passthrough / locale resolve). CMS 193/193, PM 32/32, tsc clean, opennext build gate (via bundle:cms) clean, bundle regenerated.
- DONE: Scaffold the PM Next.js app under `ProjectManager/` wired for Cloudflare Workers deployment (OpenNext / wrangler config). Hello-world + /api/health building; `opennextjs-cloudflare build` emits a worker bundle.
- DONE: Scaffold the default Next.js install under `CMS/` (mirrors PM OpenNext/wrangler wiring; `next build` + `opennextjs-cloudflare build` both pass).
- DONE: Add Cloudflare D1 binding + initial schema/migrations for users, invites, sites, site_users. (drizzle-orm/d1; schema in src/db/schema.ts, migration 0000 generated; DB+SESSIONS bindings in wrangler.jsonc with placeholder ids.)
- DONE: **UI foundation** — Tailwind v4 in `ProjectManager/` (@tailwindcss/postcss, globals.css with @theme), light+dark+system theme via purpose-named CSS-var tokens (surface/surface-muted/surface-raised/foreground/foreground-muted/border/primary/primary-hover/primary-foreground/danger/success/ring), ThemeProvider+ThemeScript(no-FOUC)+ThemeToggle respecting prefers-color-scheme & persisting choice, composable base components (`<Button>`, `<Card>` family, `<Table>` family, `<Field>`/Input/Select/Textarea) under `src/components/`, styleguide home page. Verified tsc + next build + opennextjs-cloudflare build.
- DONE: **PM i18n foundation** — next-intl v4, cookie-based EN/FI/ET (commit `c993698`). See CAVEATS "PM i18n".
- DONE: Email+password auth — first registrant → SuperAdmin; subsequent don't; sessions in KV (commit `d5cd3a0`). See CAVEATS "PM auth".
- DONE: Invite flow — SuperAdmin/`canInvite` Admins invite by email+role+country; accept-invite page; multi-country scope (empty=global); origin hardened (commits `0ce61d4`/`ea92ebf`/`cf4c245`).
- DONE: Site CRUD — create/list/detail/edit Sites + assign PM users; country-scoped authz enforced server-side (`src/lib/site/`, `src/app/(app)/sites/`). Verified tsc + opennextjs-cloudflare build.
- DOING: Site deployment — PM calls Cloudflare API to provision a CMS Worker per Site; report deploy status. Must work from the deployed PM.
  - DONE (engine core): `lib/deploy/` Cloudflare Workers Script-Upload API client + deploy orchestration state-machine (draft→deploying→deployed/failed) + data-layer `setSiteDeployStatus`. Pure parts unit-tested (`npm test`, 6/6); tsc + opennextjs-cloudflare build clean. No live CF auth → live upload not exercised.
  - DONE: **CMS bundle production** — committed pre-bundled artifact: `scripts/build-cms-bundle.mjs` esbuild-bundles `CMS/.open-next/worker.js` (+~980 chunks) into ONE ~3.9MB self-contained ESM module → committed `src/lib/deploy/cms-bundle.generated.js`; `buildCmsBundle()` loader returns the engine's `{mainModule,files}` shape. Verified tsc + 9/9 tests + opennextjs-cloudflare build. Live boot-on-Worker unverified (no CF auth). See CAVEATS "CMS bundle".
  - DONE: deploy UI — `deploySiteAction` (authz: country-reach OR site_users assignment) + Deploy/Redeploy button & status on Site detail page wired through `deploySite`+`buildCmsBundle`; `sites.deploy.*` i18n in EN/FI/ET (parity verified). tsc + 9/9 tests + opennextjs-cloudflare build clean. Live deploy path still unexercised (no CF auth).
  - TODO (next): real end-to-end deploy against a live Cloudflare account/token (set `CF_API_TOKEN`/`CF_ACCOUNT_ID` secrets; needs auth in env).
- DONE: CMS UI i18n — next-intl v4 cookie-based EN/FI/ET in `CMS/` mirroring PM (`src/i18n/{routing,request}.ts`, `messages/{en,fi,et}.json`, `LocaleSwitcher`, layout `NextIntlClientProvider`). Regenerated the committed CMS deploy bundle. Verified tsc + opennextjs-cloudflare build gate + parity; PM tsc + 9/9 tests. See CAVEATS "CMS UI i18n".
- DONE: Pre-deploy validation script (`npm run preflight`, `scripts/preflight-deploy.mjs`) — fails loudly (exit 1) on placeholder zero-ids in wrangler.jsonc (D1/KV), missing required compat flags, missing/empty/wrong-shaped CMS bundle artifact; APP_ORIGIN-unset is a warning. Self-tested (`scripts/preflight-deploy.test.mjs`, 5 cases). Verified: 14/14 tests pass; preflight correctly fails on current placeholder wrangler.jsonc. No app source touched → deploy gate unaffected.
- DONE: CMS bundle boot self-check (`npm run bundle:selfcheck` / `scripts/bundle-selfcheck.mjs`, also run inside `npm run preflight`) — static validation of the committed CMS Worker artifact beyond size: entry contract (worker.js + default export + fetch handler), unresolved-bare-import detection (only node:/cloudflare: external = ERROR), and the OpenNext **Durable Object export gap** (DOQueueHandler/DOShardedTagCache/BucketCachePurge exported but no durable_objects/migrations in the upload metadata = WARNING). De-risks the one link no offline check covered. 22/22 tests (+8). DEPLOY.md step 8/11 + troubleshooting updated. No app source touched.
- DONE: DEPLOY.md runbook (repo root) — exact ordered steps to take PM live on Cloudflare and trigger a CMS Worker deploy from the deployed PM (NEXT slice 1): login → d1/kv create → paste ids → cf-typegen → db:migrate → secrets (CF_API_TOKEN/CF_ACCOUNT_ID/APP_ORIGIN) → bundle:cms → preflight → deploy → bootstrap → in-app Site deploy + curl verify. Includes quick-ref command list + troubleshooting table. Claims cross-checked vs wrangler.jsonc/package.json/code; preflight run confirms documented behavior. Doc-only.
- DONE: Fix the live-deploy Durable Object blocker — OpenNext's CMS `worker.js` always re-exports `DOQueueHandler`/`DOShardedTagCache`/`BucketCachePurge` (config-independent), which `buildScriptUploadForm` declares no `durable_objects`/`migrations` for → Cloudflare rejects the upload. `scripts/build-cms-bundle.mjs` now strips those dead DO re-exports (caches are dummy → DOs never instantiate) before esbuild (via stdin + resolveDir); `CMS/open-next.config.ts` pins dummy caches explicitly. Regenerated bundle exports only `default`/`fetch`. Verified: bundle:selfcheck zero warnings, preflight clean (bundle part), 22/22 tests (integration test flipped to guard the strip), tsc + opennextjs-cloudflare build gate pass. DEPLOY.md/CAVEATS updated.
- DONE: Wire preflight into a `predeploy` npm hook — `"predeploy": "npm run preflight"` in ProjectManager/package.json; npm auto-runs it before `deploy` and a non-zero exit aborts the build/upload, so `npm run deploy` can't ship placeholder ids / a stale bundle. Plus froze deploy error-path i18n parity with a regression test (`scripts/deploy-i18n-parity.test.mjs`) that derives the error-key set from source (DeployErrorKey ∪ gate keys) and asserts EN/FI/ET catalogs match exactly. Verified: 26/26 tests; preflight exit-1 propagates through predeploy. DEPLOY.md step 8/9 updated. No app source touched.
- DONE: Audit Script-Upload metadata (bindings/assets) vs the DO-free bundle (NEXT slice 1) — confirmed compat-date/flags/main_module correct for a no-DO worker; FOUND the static-assets blocker: the worker serves `_next/static`/CSS/JS-chunks/public via `env.ASSETS.fetch` (Workers Static-Assets binding) but `buildScriptUploadForm` sends no `assets` metadata/ASSETS binding + the committed artifact ships no static files → a live upload boots with `env.ASSETS` undefined → static assets 404 (unstyled/dead pages). Added bundle-selfcheck check #4 (warning) + 2 unit tests + integration guard (28/28); DEPLOY.md step 11 🚧 callout + troubleshooting row; CAVEAT. `env.IMAGES` is optional (worker degrades gracefully). No app source touched.
- TODO (next deploy slice — BLOCKS a functional live deploy): **Upload CMS static assets alongside the worker.** The worker `env.ASSETS.fetch`-es `.open-next/assets` (14 files, ~700KB) but the deploy uploads only the JS module → assets 404 on a live deploy. Implement Cloudflare's **Workers Assets API**: create an upload session (`POST .../workers/scripts/{name}/assets-upload-session` with a manifest of file hashes+sizes) → PUT the missing files in buckets → receive a completion JWT → pass it as `metadata.assets.jwt` (+`config`) in `buildScriptUploadForm`. Needs the asset bytes available at deploy time — extend `build-cms-bundle.mjs` to also emit the assets (hash+content) into the committed artifact (or a sibling), since deploy runs from the deployed PM and can't read `.open-next/`. Verify request-building offline (unit-test the manifest/metadata shape); the live PUT needs a CF account. Alternative: switch deploy to wrangler's assets-aware upload. See DEPLOY.md step 11 🚧 + CAVEAT "static-assets gap".
- SUPERSEDED (M1 deploy now works live via the deployer Container, not Script-Upload — these Script-Upload/asset-upload tasks are moot): the two "Upload CMS static assets via Workers Assets API" / "real end-to-end deploy" TODOs above. The container path (`opennextjs build && wrangler deploy`) handles assets+bindings natively. See memory `pm-cms-deploy-via-container`.
- DONE: Stuck-deploy detect/cancel/restart — `deploy_started_at` column + `isDeployStuck` (10-min threshold) + stale-aware `canStartDeploy` + `/deploy/cancel` route + UI badge/Cancel/Redeploy. Pure state predicates extracted to `deploy-state.ts` (4 tests). PM 0.2.0, live (commit `37e7717`).

## Milestone 2 epics — AI-assistant CMS (FIRST PASS, narrow/vertical, still refining with user)
States: TODO | DOING | DONE | BLOCKED. Each is ONE provable capability. Order is rough dependency order, not commitment. Discuss scope before picking.

**A. Rendering foundation (no AI yet — make the artifact model real)**
- DONE: **A1 — D1 schema for components + pages.** Added drizzle/D1 layer to the CMS app (`CMS/src/db/{schema,index}.ts`, `drizzle.config.ts`, `db:generate`, migration `0000_worried_nextwave.sql`, `DB` d1 binding in wrangler.jsonc). `component`(name UNIQUE, tree/script/css, props_schema) — the artifact, NOT JSX source; `page`(slug, parent_page_id tree, display_order, publish_status, blocks JSON, per-locale meta_title/description) with UNIQUE(parent_page_id,slug). NO entity tables. Verified: drizzle generate, tsc (post cf-typegen), opennextjs-cloudflare build gate, PM bundle regen, 3 dep-free regression tests. Live D1 apply unverified (no CF auth).
- DONE: **A2 — block-tree renderer + public page route.** `CMS/src/app/[[...slug]]/page.tsx` loads a published page from D1, walks its block tree, SSRs each component's `tree`, ships its `script`. Replaced the `/test` proof with real data-driven pages.
  - DONE (de-risk core): pure render-plan walker `CMS/src/lib/render/tree.ts` (`planTree`/`planPage`/`parseJsonColumn`, React-free, 11 dep-free tests) + React adapter `react.tsx` (`renderPlan`/`renderPlans` via createElement). tsc + 14/14 tests + opennextjs build gate clean; PM bundle regen. See JOURNAL 2026-06-17 20:56.
  - DONE (route wiring): `[[...slug]]/page.tsx` — `getDb()` → walk parent/child tree to the published leaf page (`resolveSlugPath` pure helper in `lib/render/slug.ts`, root `/`→`home`), `parseJsonColumn` blocks, `inArray(component.name, names)` → component Map, `planPage` → `renderPlans` + scripts as `<script>`; `generateMetadata` resolves per-locale metaTitle/Desc; `notFound()` on miss. Deleted old static `page.tsx`+`test/page.tsx`. Verified: CMS 22/22 tests (+8 slug), tsc clean, opennextjs build gate PASS (`ƒ /[[...slug]]`), PM bundle regen + 32/32. Live D1 render unverified (no CF auth). See JOURNAL 2026-06-17 21:00.
- DONE: **A3 — precompiled Tailwind utility sheet.** `CMS/src/lib/render/utility-css.ts` (pure, node-testable) defines the AI's BOUNDED allowed class vocabulary + generates the matching CSS (color utils → the same `--color-*` purpose-token vars as globals.css, so it follows light/dark theme; no hex/oklch/scale literals). Injected as an inline `<style>` on the public route `[[...slug]]/page.tsx` (inline = no static-asset upload needed → sidesteps the ASSETS deploy gap). Proven: classes never written in JSX (`bg-primary-subtle`/`max-w-prose`) still get CSS. CMS 30/30 tests (+8 utility-css.test.mjs), tsc + opennextjs build gate clean, PM bundle regen + 32/32.

**B. The AI assistant (the core)**
- DONE: **B1 — chat endpoint + streaming (no tools).** `CMS/src/app/api/chat/route.ts` (`POST(Request)`→`text/event-stream` `Response`, REST-only) calls Workers AI `env.AI.run(model,{messages,stream:true},{gateway:{id}})` behind AI Gateway and re-frames the upstream OpenAI-style SSE into a stable client protocol (`token`/`done`/`error`) via the PURE, unit-tested `CMS/src/lib/chat/sse.ts` (`SseDeltaParser` cross-chunk incremental parse + `frameEvent` + `parseChatBody`). `AI` binding + `AI_GATEWAY` var added to wrangler.jsonc; conversation state client-side; NO tools (that's B2). Verified: CMS 45/45 (+15 chat-sse.test.mjs), tsc + opennext gate clean (`ƒ /api/chat`), PM bundle regen + 32/32. Live model call needs a real AI binding + gateway (HITL: P1 create gateway/enable Workers AI, P2 curl SSE). See JOURNAL 2026-06-17 21:11.
  - ⚠️ RISK (carry into B2): the agentic tool loop needs strong **tool-calling**. Workers AI supports function calling, but its open models are weaker at multi-step tool use than frontier models. AI Gateway makes this swappable (point at Workers AI OR Anthropic OR fallback, same code) — so if Workers-AI tool-calling can't reliably emit `{tree,script,css}` + chain tools, route that path to a stronger model via the gateway WITHOUT re-architecting. Decide empirically at B2.
- DONE: **B2 — tool: create/update component.** First tool — AI emits `{tree,script,css}`, validated + written to D1 (A1). Proves: the AI can author a custom component that then renders via A2.
  - DONE (offline core): tool schema (`CREATE_COMPONENT_TOOL`) + tool-call SSE delta accumulator (`sse.ts` `extractToolCall`/`ToolCallAccumulator`, new `tool_call` event) + PURE artifact validator (`component-tool.ts` `validateComponentArtifact`: tree via `planTree`, classes via `allowedClasses()` root+nested, name regex, 64KB script bound) + D1 upsert (`db/component-store.ts` `upsertComponent`, name UNIQUE → update-or-insert) + route wired (`tools:[…]`, ONE tool round → `tool` event). CMS 61/61, tsc + opennext gate clean, PM bundle regen + 32/32. Live model tool-call + D1 write → HITL (P1). See JOURNAL 2026-06-17 21:19 + CAVEATS "CMS create_component tool (B2)".
  - DEFERRED (live path): the full multi-turn **agentic loop** — feed the `tool` result back to the model for a follow-up turn (so it can chain create→compose, or fix a rejected artifact). Currently the route runs a SINGLE tool round then `done`. Decide if one round suffices after live-testing the model's tool-calling (HITL P1).
- DONE: **B3 — tool: create/compose page.** AI assembles components into a page block tree (A2). Second AI tool, mirrors B2: `CMS/src/lib/chat/page-tool.ts` (`CREATE_PAGE_TOOL` schema + PURE `validatePageInput`: slug regex, optional parentSlug, publishStatus enum, recursive block-tree shape via `planPage`, accepts blocks as array or JSON-string, per-locale meta maps, returns referenced `componentNames`) + `CMS/src/db/page-store.ts` (`missingComponents` existence check + `upsertPage` resolves parentSlug→id, upsert by UNIQUE(parent_page_id, slug)) + route `TOOLS`/`runTools` refactored to a name-dispatcher (`handleCreateComponent`/`handleCreatePage`; create_page verifies components exist before write). CMS 79/79 (+18), tsc + opennext gate clean, PM bundle regen + 32/32. Live model tool-call + D1 → HITL P1. See JOURNAL 2026-06-17 21:24.
- DONE: **B4 — tool: translate** (offline core; commit this run). `translate(kind,target,fields)` tool: PURE `lib/chat/translate-tool.ts` (`CREATE_TRANSLATION_TOOL` schema + `validateTranslationInput` — locale-object shape validated against the Site's `getContentLocales` set + `mergePageFields` non-mutating page merge: metaTitle/metaDescription + `<blockId>.<propName>` → locale objects `resolveLocalized` renders), D1 write `db/translate-store.ts` `applyTranslation` (page lookup-by-slug → merge → write; component targets rejected for now), route `TOOLS` + `handleTranslate` branch passing allowedLocales. CMS 111/111 + tsc + gate; PM bundle regen + 32/32 + gate. Live model + D1 → HITL P1.

**No entity types — content is generic.** A "blog post" is just a Page; a "blog" is a Page that lists other Pages. No blog/product/artwork/order tables or CRUD. The only blog-specific thing is a premade component set (G1).

**Do NOT port any aicms ENTITIES.** Mining `../aicms` is for the generic *mechanics* — page/block tree, content-i18n resolution, R2 upload, settings KV pattern — NEVER its domain tables (artwork/product/blogPost/discount/order) or their tools. bizbeecms content is generic Pages + components; everything domain-specific is composed, not modeled.

**C. Content depth (mine from aicms)**
- DONE: **C1 — per-Site content locales (offline core).** Data-driven content-language set (distinct from EN/FI/ET admin UI). PURE `lib/render/localize.ts` (`resolveLocalized` deep locale-object resolution + `normalizeContentLocales` config model + code validate/normalize); wired into `tree.ts` `planTree`/`planPage` via optional `LocaleContext`; stored in new generic `site_settings` D1 table (`db/settings-store.ts`, migration 0001); public route resolves blocks + metaTitle/Desc per content locale w/ fallback. CMS 95/95 (+16), tsc + opennext gate clean, PM bundle regen + 32/32. Live D1 unverified (no CF auth). See JOURNAL 2026-06-17 21:30.
- DONE: **B-track chat UI — CMS admin chat page (`/admin/chat`).** Client page POSTs to `/api/chat`, consumes the `token`/`tool`/`done`/`error` SSE protocol (pure node-tested parser `CMS/src/lib/chat/client-sse.ts` = `ChatEventParser`/`parseFrame`, mirrors server `frameEvent`), renders streaming transcript + green/red tool-result cards. `CMS/src/components/chat/admin-chat.tsx` (client) + `CMS/src/app/admin/chat/page.tsx` (server, explicit route beats `[[...slug]]`). EN/FI/ET `chat` namespace. CMS 127/127 (+16) + tsc + gate; PM bundle regen (4641KB) + 32/32 + selfcheck. Live round-trip = existing HITL P1 (needs AI binding+gateway). See JOURNAL 2026-06-17 21:51.
- DONE: **C1b — content-locale settings UI.** Explicit `/admin/settings/content-locales` page + `"use client"` `content-locales-editor.tsx` (list/add/remove/make-default, native controls + purpose tokens) + REST `api/settings/content-locales/route.ts` (GET/PUT, server re-normalizes, no server actions) + `contentLocales` i18n namespace (EN/FI/ET parity). CMS 130/130 (+3 `content-locales-ui.test.mjs` = key-parity + normalize invariants) + tsc + gate (both routes registered); PM bundle regen (5002KB) + 32/32 + selfcheck. Live D1 → HITL P1. See JOURNAL 2026-06-17 22:12.
- DONE: **C2 — page management UI (metadata).** `/admin/pages` list/create/edit/delete by slug + parent + publish status + per-locale SEO (non-AI counterpart to B3 create_page; blocks left to AI/C3). PURE `lib/pages/page-meta.ts` (`validatePageMeta`/`isValidSlug`) + page-store `listPages/getPageById/upsertPageMeta/deletePage` (parent resolve, UNIQUE(parent,slug) guard, no-orphan delete) + REST `api/pages` (GET/POST/PUT/DELETE) + admin page + `pages-manager.tsx` client + EN/FI/ET `pages` namespace. CMS 154/154 (+6), tsc+gate, PM bundle 5508KB + 32/32. Live persistence → existing HITL P1 (deploy). See JOURNAL 2026-06-18 05:13. (Still TODO: nav-menu builder + redirects + displayOrder reorder UI — deferred.)
- DONE: **C3 — section/block editing UI (vertical slice).** NON-AI visual compose/reorder of a page's TOP-LEVEL block tree — the missing half of C2 (metadata) + the editor counterpart to B3. PURE `lib/pages/page-blocks.ts` (`validateBlocks` via renderer `planPage` + unique-id guard; `addBlock`/`removeBlock`/`moveBlock` immutable ops); page-store DEDICATED block write contract `setPageBlocks` (blocks-only, NOT upsertPageMeta) + `getPageBlocks` + `listComponentNamesForPalette`; REST `api/pages/[id]/blocks` GET/PUT (checks `missingComponents` before persist); `/admin/pages/[id]/blocks` page + `block-editor.tsx` (palette → add, ↑/↓ reorder, remove, save); "Edit blocks" link per page in PagesManager; EN/FI/ET `pageBlocks` namespace. CMS 175/175 (+7), tsc+gate (both routes registered), PM bundle 5938KB + 32/32 + selfcheck. Live D1 → HITL P1. See JOURNAL 2026-06-18 09:41. STILL TODO (deferred): nested block.children editing, per-block props editing + block-prop→component-prop binding, drag-and-drop reorder.

**D. Assets**
- DONE: **D1 — R2 asset upload + gallery (offline core).** Native R2 binding `MEDIA` (no presigning — aicms presigns only because it runs off-CF). PURE `CMS/src/lib/render/asset.ts` (validate type+size, collision-resistant key, traversal-guard, `assetUrl`→`/media/<key>`); `asset` D1 metadata table (migration 0002) for listing; `db/asset-store.ts` (R2+D1 list/put/delete/get); REST `api/assets/route.ts` (GET/POST-multipart/DELETE); serve route `app/media/[...key]/route.ts` (streams R2, immutable cache); admin `app/admin/media/page.tsx` + client `components/media/media-gallery.tsx` (upload/grid/copy-URL/delete); `media` i18n EN/FI/ET. CMS 139/139 (+9 `asset.test.mjs`), tsc + opennext gate (3 routes registered); PM bundle regen 5269KB + 32/32 + selfcheck. Live R2/D1 → HITL P1 (provision per-Site bucket + apply migration 0002). See JOURNAL 2026-06-17 22:20.
- DONE: **D1 loop-closer — `list_assets` AI tool.** 5th AI tool: read-only `list_assets` returns the Site's uploaded media as `/media/<key>` URLs so create_component/create_page reference real assets, not placeholders. PURE `lib/chat/list-assets-tool.ts` (schema + `coerceLimit` + `formatAssetList`) + route dispatch `handleListAssets` (calls `listAssets()`, emits `tool {ok,assets}`) + client-sse `ToolResult.assets`. CMS 148/148 (+7), tsc + gate; PM bundle regen 5274KB + 32/32 + selfcheck. Live → HITL P1. See JOURNAL 2026-06-18 08:02.
- TODO: **D2 — Cloudflare Images transforms (optional).** Resize/format on serve. Defer until D1 lands.

**E. Settings & theming**
- DONE: **E1 — per-Site theme overrides.** DB-backed CSS-var overrides injected as inline `<style>` after globals on the public route. PURE `CMS/src/lib/render/theme.ts` (token allowlist `THEME_TOKENS` synced to globals.css `--color-*` + narrow color-value grammar `isSafeColorValue` = SECURITY boundary since values go into an inline `<style>` + `normalizeThemeOverrides`/`themeOverridesToCss`); `db/settings-store.ts` `getThemeOverrides`/`setThemeOverrides` (key `theme_overrides`, reuses new shared `upsertSetting`); REST `api/settings/theme/route.ts` (GET/PUT); public `[[...slug]]` injects `themeCss` `<style>` after UTILITY_CSS; admin `/admin/settings/theme` page + `components/settings/theme-editor.tsx` (color swatch + text field per token); `theme` i18n EN/FI/ET. CMS 161/161 (+7 `theme.test.mjs`), tsc + opennext gate (2 routes registered); PM bundle regen 5591KB + 32/32 + selfcheck. Live D1 → HITL P1. See JOURNAL 2026-06-18.
- DONE: **E2 — site settings (brand identity / design system / AI persona).** PURE `lib/settings/site-settings.ts` (`SiteIdentity` {brandName,tagline,voice,design,aiPersona} + `normalizeSiteIdentity` trim/clamp + `buildSystemPrompt`) → `site_settings` row key `site_identity` (`getSiteIdentity`/`setSiteIdentity`, reuses shared `upsertSetting`) → REST `api/settings/brand` → `/admin/settings/brand` page + `brand-editor.tsx` → EN/FI/ET `brand` namespace. **Wired into the AI system prompt**: `api/chat/route.ts` `withSystemPrompt` prepends identity + `listComponentNames()` + sorted `allowedClasses()` + tool guidance (mentions list_assets + components, create-components-before-pages, /media URLs). CMS 168/168 (+7), tsc + opennext gate (both routes registered), PM bundle 5682KB + 32/32 + selfcheck. Live model/D1 → existing HITL P1. See JOURNAL 2026-06-18 09:34.

**Sec. Security (cross-cutting)**
- DONE (2026-06-18): **Sec1 — CMS admin authentication.** Built per the decided arch (reuse PM's `bizbee_session`/KV via PM `/api/auth/cms-validate`; CMS `requireAdmin` guard forwards cookie + `env.SITE_ID` + `CMS_AUTH_SECRET` bearer; authorize via PM's site-reach check). ALL admin `/api/*` routes gated + `/admin/*` layout guard + deployer `--var` injection + EN/FI/ET `adminAuth` strings. CMS 217/217, both gates pass. Live round-trip → HITL P1. See JOURNAL 2026-06-18 12:33 + CAVEAT "CMS admin auth IS BUILT".
- DONE: **Sec2 — media serve stored-XSS hardening.** `/media/[...key]` now sets `x-content-type-options: nosniff` always and, for SVG (an active document that can carry `<script>`), `Content-Security-Policy: default-src 'none'; sandbox` + `Content-Disposition: attachment` so a user-uploaded SVG can't execute in the CMS origin / reach admin cookies (still renders fine inside `<img>`). Pure decision in `assetServeHeaders` (3 tests). Found by security review 2026-06-17.

**F. Fleet ops (PM side)**
- TODO: **F1 — deploy fleet view.** PM dashboard listing all Sites' deploy status + stuck flags at a glance (extends the per-site stuck detection we shipped to fleet-wide). Proves: an operator spots every stuck/failed deploy on one screen.

**H. Component portability (export/import across Sites)**
> Architecture fit: a component is already pure data (`{tree,script,css,propsSchema}` — no eval, no binary), so export = serialize a D1 row to JSON, import = insert into another Site. The no-server-eval decision is what makes this trivial. Premade kits (G) are just curated import bundles — same machinery.
- DONE (2026-06-18): **H1 — export a component.** PURE versioned format `lib/components/portable.ts` (`PORTABLE_FORMAT="bizbeecms.component"` v1; `serializeComponent(row)`). REST `GET /api/components?name=X` downloads the bundle; `/admin/components` "Export" button per component.
- DONE (2026-06-18): **H2 — import a component.** `parsePortableComponent` is the import TRUST BOUNDARY — re-runs the AI path's `validateComponentArtifact` gate (renderable tree, allowed-class allowlist, bounded script, safe name) + envelope/version + propsSchema-JSON bound. REST `POST /api/components` validates → `upsertImportedComponent` (by unique name, also persists propsSchema). `/admin/components` paste + .json upload UI.
- DONE (2026-06-18): **H3 — asset-URL dependency handling.** Export declares `assets: string[]` (safe `/media/<key>` deps via `enumerateAssetDeps`); import rebinds via `parsePortableComponent(raw,{rebind})` through the SAME gate + returns remaining deps; REST + read-only UI surface them. REMAINING (new TODO below): editable rebind UI + nested-component (component-references-component) dep warning.
- DONE (2026-06-18): **H3b part 2 — nested-component dep enumeration + missing-component warning (correctness gap).** PURE `enumerateComponentDeps(tree)` in `portable.ts` (PascalCase `tag` = component ref, `COMPONENT_TAG_RE`); export envelope + parse carry `componentDeps` (self-ref filtered, re-enumerated from validated artifact on import); `db/component-store.ts` `missingComponentNames` (inArray set-diff); `/api/components` + `/api/components/kit` return `missingComponents`; danger-toned "Missing component dependencies" warning panel in components-manager (warn, don't auto-install). EN/FI/ET `componentDepsTitle`/`componentDepsHint`. CMS 206/206 (+5), tsc + opennext gate, PM bundle 6220KB + 32/32 + selfcheck.
- TODO: **H3b part 1 — editable asset-rebind UI** (deferred from the H3b slice). In `components-manager.tsx`: after a paste/upload/kit response that surfaces `assets`, render an EDITABLE per-dep control (keep / rebind to a `/media/<key>` picked from `GET /api/assets` / drop) → build the `{rebind}` map → POST it (the route ALREADY accepts `{rebind}`; the pure validator shipped in H3). No new write path — just feed the existing rebind param. Cross-check declared `assets` vs the Site's `/api/assets` to show which are missing vs present. Purely an editor-UI expansion; the format/validator/REST are done.
- (vision) business developers managing many client sites build a personal/shared library and import their components into each new Site. A future shared registry is the natural extension (defer).

**G. Premade component kits (starter library)**
> Built on H — a kit is a curated, versioned export bundle the CMS ships with and can import into any Site.
- DONE (2026-06-18): **G1 — blog component kit.** 5 premade components authored AS `bizbeecms.component` v1 portable bundles in `CMS/src/lib/components/blog-kit.ts` (BlogPostHeader, BlogPostBody, AuthorCard, PostListItem, PostList). One-click install via `POST /api/components/kit` (`src/app/api/components/kit/route.ts`) which re-validates EVERY bundle through `parsePortableComponent` then upserts via `upsertImportedComponent` — SAME gate + write path as manual import, NO new path. "Install blog kit" button in ComponentsManager. EN/FI/ET keys + `scripts/blog-kit.test.mjs` (5 tests). Build gate green, bundle regenerated. PostList is a static demo container (real posts via page blocks); follow-ons in NEXT.
- (later) Gxx — other kits (landing/marketing, docs, portfolio) follow the same pattern once G1 validates the approach.

**UX. Admin shell**
- DONE (2026-06-18): **Shared /admin nav + index page (NEXT slice #6).** Persistent `AdminNav` client component (active link via `usePathname`, Home + chat/pages/components/media/settings links + LocaleSwitcher) rendered in the auth-allowed branch of `/admin/layout.tsx` (guard intact). New `/admin` index page (force-dynamic, try/catch → safe fallback) grids each section with a one-line description from a single `ADMIN_SECTIONS` const shared with the nav. Settings sub-pages grouped under ONE "Settings" link → content-locales (simpler option). EN/FI/ET `adminNav` namespace (key-parity test-locked, `scripts/admin-nav.test.mjs`). CMS 219/219 (+2), tsc + opennext gate (`ƒ /admin` registered), PM bundle 6470KB. See JOURNAL 2026-06-18 12:39.

**Z. Future / parked (TBD — not scoped, just don't forget)**
- TBD: **Z1 — shop / product management.** E-commerce: products, cart, checkout, orders. Explicitly deferred — revisit after the core AI-CMS (A–H) proves out. When picked up, decide whether it's first-class entities (the one place real domain tables might be justified) or still composed from generic content + components. No design yet.

### Open questions to discuss before committing the backlog
- Sequencing: do A (rendering) fully before B (AI), or interleave A1→A2→B1→B2 as one vertical thread?
- ~~entity types~~ RESOLVED: no entities — content is generic Pages; "blog" = a page that lists pages + the G1 premade component kit.
- "PostList lists child pages" — how does a page know it's a blog post? A tag/type field on `page`, or just parent-child hierarchy (children of the blog page = posts)? (affects A1 schema + G1)
- ~~AI provider~~ RESOLVED: Cloudflare-native — Workers AI (`env.AI`) behind AI Gateway; swappable to a stronger model via the gateway if tool-calling needs it (see B1 risk).
- Is the non-AI manual editor (C2/C3) needed for v1, or is the AI the only authoring path initially?

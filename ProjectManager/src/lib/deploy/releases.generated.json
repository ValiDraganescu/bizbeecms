{
  "releases": [
    {
      "version": "3.2.1",
      "tag": "r-3.2.1",
      "markdown": "# CMS v3.2.1\n\n_2026-08-20 · changes since r-3.2.0_\n\n## Other\n- Maintenance re-cut of v3.2.0 to exercise the new fleet rollout pipeline.\n  No functional changes for site editors.\n"
    },
    {
      "version": "3.1.2",
      "tag": "r-3.1.2",
      "markdown": "# CMS v3.1.2\n\n_2026-08-19 · changes since r-3.1.1_\n\n## Other\n- Build housekeeping: dropped the unused `next lint` script. No runtime changes.\n"
    },
    {
      "version": "3.0.0",
      "tag": "r-3.0.0",
      "markdown": "# CMS v3.0.0\n\n_2026-08-18 · changes since r-2.9.0_\n\n## Breaking\n- **API keys are gone.** The remote MCP server (`/mcp`) no longer accepts `Authorization: Bearer bzb_…` keys, and Settings → MCP no longer mints them. Any Claude Code / `.mcp.json` entry that carried a key must drop the header — after this release the client authenticates with OAuth instead (see below). The `api_key` table is dropped on migration.\n\n## Features\n- **OAuth 2.1 sign-in for MCP.** Each site is now its own authorization server: add just the site's `/mcp` URL to Claude Code (no secrets, safe to commit), run `/mcp` → Authenticate, and the browser opens the site's new consent page. You sign in with your normal CMS account (password, Google, or \"Sign in with BizbeeCMS\") and approve; the client acts *as you*, bounded by your role. Tokens are short-lived with rotating refresh, PKCE-bound, and hashed at rest.\n- **Consent page** (`/oauth/authorize`) shows who is asking, which account will be acting, what that allows, and where the request returns. Signed-out visitors get the site's login page in place and land back on the request afterwards.\n- **Settings → MCP** now lists your authorized clients (created / last used / expiry) with one-click revoke, plus copy-pasteable Claude Code wiring for this site.\n\n## Other\n- OAuth and MCP responses are stamped `private, no-store` at the edge and excluded from link localization.\n"
    },
    {
      "version": "2.9.0",
      "tag": "r-2.9.0",
      "markdown": "# CMS v2.9.0\n\n_2026-08-18 · changes since r-2.8.0_\n\n## Features\n- Project Manager exposes an MCP endpoint (`/mcp`) protected by a built-in OAuth 2.1 authorization server, so MCP clients (Claude Code, etc.) can connect to your PM account; a new **Settings → Connections** page lists and revokes authorized clients.\n- New Site form auto-generates the slug from the name as you type and checks live that it's not already taken.\n\n## Other\n- Custom domain panel: removed the generic \"Setting up a custom domain\" guide — the panel now shows only the exact DNS records for the domains you entered.\n"
    },
    {
      "version": "2.8.0",
      "tag": "r-2.8.0",
      "markdown": "# CMS v2.8.0\n\n_2026-08-18 · changes since r-2.7.0_\n\n## Features\n- **Custom domains: certificates are now fully automatic.** A customer only adds the routing DNS record (CNAME to `cf.bizbeecms.com`, or the apex A records). As soon as it resolves to us, Cloudflare validates the domain over HTTP, issues the HTTPS certificate and renews it forever — there are no more `_acme-challenge` TXT or DCV-delegation records to add, and no \"validate your domain to renew\" emails.\n- The Site's **Custom domain** panel now shows the certificate state (active / pending) with a **Check status** button instead of validation records.\n\n## Other\n- Domains attached before this release are upgraded to the automatic flow on their next redeploy or when re-checked from the panel.\n- No CMS runtime changes in this release; the CMS bundle is identical to 2.7.0.\n"
    },
    {
      "version": "2.7.0",
      "tag": "r-2.7.0",
      "markdown": "# CMS v2.7.0\n\n_2026-08-18 · changes since r-2.6.1_\n\n## Features\n- **Full CRUD over MCP / AI chat**: data sources, saved requests, collections, collection fields, collection items and chat agents can now be *updated* and *deleted* by the AI assistant and MCP clients — not just created. All updates follow one patch contract: omitted = keep, `null` = reset/clear.\n  - `update_data_source` (partial patch, write-only secret rotation, auth transitions), `set_data_source_request` (upsert with per-key query merge, stable ids), `delete_data_source` / `delete_data_source_request`.\n  - `update_chat_agent` no longer resets model/limits when you only change the system prompt; `set_chat_agent_data_source` / `_collection` are merge patches; per-locale welcome messages merge.\n  - `update_collection` (label, description, public submissions), `update_collection_field` (description/required/default/type — type changes ride the schema rebuild and report the exact coercion applied), `delete_collection`, `restore_collection_item`, and permanent `delete_collection_item` (distinct from archive).\n- **Guarded deletes**: deleting a data source, request or collection is refused while pages, components or chat agents still reference it — the error names every referencing entity and the tool that removes the reference. Guards fail closed on unreadable page data.\n- **`edit_text` gets a `replaceBetween` mode** (exact start/end markers, occurrence, near-miss hints) and new targets: chat-agent system prompt / welcome message, data-request body template, and text/richtext collection item fields.\n- **Authoring guides updated** (data sources, chat agents, tool scopes) to describe the patch surface, guarded deletes and the new edit_text targets.\n\n## Fixes\n- `bodyTemplate` on data-source requests is now validated to be valid JSON *after* placeholder substitution, across create/update/edit_text and the Admin REST routes.\n- `update_chat_agent` rejects unknown `limits` keys by name instead of silently resetting every limit.\n- `update_collection_field required:true` over rows with NULLs is pre-checked and back-filled from the supplied default in the same call.\n\n## Other\n- Custom-domain certificates: after each Site deploy, active custom hostnames still on TXT validation are switched to HTTP validation so cert renewals complete without the customer re-adding `_acme-challenge` TXT records (fixes the \"validate your domain to renew\" emails).\n- Operator tooling: `scripts/prod-connect.mjs` / `/prod-connect` skill to run a local CMS/PM against a Site's production D1/R2 for debugging, and restore local.\n- Internal: tool-dispatch split into per-domain handler modules; chat-agent tool schemas separated; shared arg validators.\n"
    },
    {
      "version": "2.6.1",
      "tag": "r-2.6.1",
      "markdown": "# CMS v2.6.1\n\n_2026-07-25 · changes since r-2.6.0_\n\n## Other\n- Settings menu reorganized into 5 clearer groups. SEO and crawler tools (SEO audit, robots, llms.txt, verification, rate limiting) now live under a dedicated **SEO & Crawlers** group, and the old \"Brand & AI\" entry is renamed to **Brand**. No pages moved and no links changed — just easier to find things.\n"
    },
    {
      "version": "2.5.0",
      "tag": "r-2.5.0",
      "markdown": "# CMS v2.5.0\n\n_2026-07-24 · changes since r-2.4.0_\n\n## Features\n- **Per-component \"Translate missing\" button.** The block inspector now has a one-click button next to the component name that fills every missing translation across all of that block's text props at once — it only appears when something is missing and disappears when done.\n- **Lists: translate the item component's fixed texts.** A List's item-component text props that aren't mapped to a data field (e.g. a \"New!\" badge or \"Book a table\" button label) are now editable per language in a new \"Item text\" section of the List settings — with the same language tabs and AI translate as everywhere else. Every row renders them in the visitor's language.\n\n## Fixes\n- Two translations running at the same time on one block (e.g. title and subtitle) no longer overwrite each other — whichever finishes last used to reset the other's result, and could also wipe text typed while a translation ran.\n- The page-level \"Translate missing\" now also covers text props that were never edited and still show their component default — previously those were silently skipped until touched once. All three translate buttons (per field, per block, per page) now use the same rule.\n- The AI-assistant chat bubble no longer covers the last controls of the settings panel — they now scroll clear of it.\n\n## Other\n- The block-binding settings screen was split from one 1600-line file into focused per-panel files (List, Form, binding controls) for maintainability.\n"
    },
    {
      "version": "1.28.0",
      "tag": "r-1.28.0",
      "markdown": "# CMS v1.28.0\n\n_2026-07-22 · changes since r-1.27.0_\n\n## Features\n- Guest chatbots now remember their conversation server-side: what the bot's tools already learned (looked-up records, policies) carries across messages instead of being re-fetched every turn, making replies faster, cheaper, and free of \"let me look that up again\" repetition.\n- Chat requests are provider-cache friendly: the payload for a conversation only ever grows (stable system prompt, append-only transcript), so prompt caching keeps repeat-message costs down.\n- Data-source tools on a chat agent can declare `requiredParams` — parameters the guest bot must always pass non-empty. An under-specified call (e.g. a search without a date window) is refused by the server with a corrective error, whatever the prompt says.\n- Chat-agent welcome messages can be localized: provide a per-language greeting (locale object) and the widget greets each visitor in their own content locale, with fallback to the site default. The admin editor gains a per-locale input with the standard language tabs.\n\n## Fixes\n- Chat widget: when one assistant reply spans several tool rounds, the text after a tool call now starts on a new paragraph instead of running on inline after the previous sentence.\n- Guest tool results are no longer over-truncated: the guest cap now matches the shared 15k sample bound, and the truncation notice tells the guest bot to narrow its filters instead of referencing an admin-only field it can't see.\n"
    },
    {
      "version": "1.27.0",
      "tag": "r-1.27.0",
      "markdown": "# CMS v1.27.0\n\n_2026-07-22 · changes since r-1.26.0_\n\n## Features\n- Chat agents get a full-page editor at Admin → Chat Agents, covering the system\n  prompt, model, tool allowlists, limits, and guest-chat appearance settings.\n- Guest conversations are now stored per agent and browsable (with JSON export)\n  under the agent's new Conversations tab — review exactly what visitors asked\n  and how the agent answered, tool calls included.\n\n## Fixes\n- When an external API rejects a request, chat agents and the admin\n  test-data-source tool now see the upstream status and the API's own error\n  message instead of a generic \"request could not be completed\" — so an agent\n  can correct a bad parameter instead of retrying blindly.\n- Optional API query parameters left empty are now omitted from the request\n  instead of being sent as empty values, which some APIs reject.\n"
    },
    {
      "version": "1.26.0",
      "tag": "r-1.26.0",
      "markdown": "# CMS v1.26.0\n\n_2026-07-21 · changes since r-1.25.4_\n\n## Features\n- **Guest chat agents.** A new *Chat Agents* admin page lets you configure chatbots that talk to logged-out visitors on your published pages. Each agent has its own system prompt, model, welcome message, usage limits, and an explicit allowlist of the data-source requests and collections it may touch.\n- **GuestChat block.** Place a chat agent on any page from the Page Builder — as an inline panel or a floating bubble. The visitor never sees or chooses the agent's model, prompt, or tools: the server re-reads the block from the published page and loads its configuration.\n- **Safe by default for guests.** A guest bot's queries only ever see published content, and anything it creates or updates lands as a draft for you to review. Per-IP and per-site rate limits (per minute, per day, per conversation) cap abuse and cost, and token usage is counted per day.\n- **The admin assistant can manage agents.** Ask the AI assistant on the Chat Agents page to list, create, update, or delete an agent; it has a built-in playbook covering limits, allowlist shapes, and the safety model.\n- **More file types in the media gallery.** PDF, plain text, Markdown, CSV, and JSON uploads are now accepted alongside images. Files whose type the browser doesn't report — `.md` most often — are recognised from their filename instead of being rejected.\n"
    },
    {
      "version": "1.25.4",
      "tag": "r-1.25.4",
      "markdown": "# CMS v1.25.4\n\n_2026-07-21 · changes since r-1.25.3_\n\n## Fixes\n- \"Sign in with BizbeeCMS\" works again on deployed Sites. The login page built\n  the SSO return URL with `http://` (the in-Worker proxy hop mis-reports the\n  protocol), which the manager rejected with `badReturn`. The return URL is now\n  always `https://`.\n\n## Other\n- OpenRouter is now the sole AI provider. The Workers AI fallback and the AI\n  Gateway plumbing were removed — every Site deploys with an OpenRouter key, so\n  the fallback was unreachable. If a Site somehow has no key, the AI assistant\n  endpoints now respond 503 instead of silently switching providers.\n"
    },
    {
      "version": "1.24.0",
      "tag": "r-1.24.0",
      "markdown": "# CMS v1.24.0\n\n_2026-07-07 · changes since r-1.23.0_\n\n## Features\n- Per-Site `robots.txt`: a settings editor with a safe default (crawlers allowed, `/admin` `/api` `/preview` disallowed) plus a free-text override and an auto `Sitemap:` pointer.\n- IndexNow: publishing, unpublishing, deleting or renaming a page automatically notifies search engines of the changed URLs (per-Site key served at a fixed path).\n- 301 redirects: auto-captured when a page is renamed, plus a manual redirects admin UI.\n- Per-page SEO controls: a `noindex` toggle (also excludes the page from the sitemap and IndexNow), full Open Graph / Twitter cards, per-Site search-engine verification tokens (Google / Bing / Yandex), and automatic BreadcrumbList JSON-LD for nested pages.\n- JSON-LD authoring: a JSON-LD component kind with a Develop editor (kind toggle, template editor, live JSON preview), List → schema.org `ItemList` aggregation, and an on-demand JSON-LD authoring guide for the AI.\n- `/llms.txt` AI-crawler index and per-page `.md` variants for AI crawlers, with an editable `llms.txt` template (variable slots) and settings editor.\n- SEO audit report: an admin page flagging orphan pages, broken internal links, missing meta, and missing image alt text — now scanning inside referenced component trees too. Plus AI tools to audit and bulk-fix missing meta and missing alt text.\n- Responsive images: width variants served from `/media?w=`, `srcset`/`sizes` on rendered images, image dimensions captured at upload and stamped through for CLS stability (including AI-generated images).\n- Open Graph images: auto-generated OG images with fallback serving and a regenerate button.\n- A designated branded 404 page that renders in the visitor's URL locale, and a per-Site rate-limit threshold (Off / Normal / Strict) enforced at the edge on public page paths.\n\n## Fixes\n- Content-locales save now purges the sitemap and `/llms.txt` caches.\n"
    }
  ]
}
